---
phase: 01-starter-pack-board-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/test/app-shell.smoke.test.tsx
  - src/vite-env.d.ts
  - src/styles/tokens.css
  - src/styles/global.css
  - src/test/setup.ts
  - src/types/puzzle.ts
autonomous: true
requirements:
  - PACK-01
must_haves:
  truths:
    - "The app boots as a React and TypeScript web app with a passing test command before feature tests exist."
    - "The repo has a stable placeholder shell that respects the Phase 1 read-only boundary."
    - "Shared puzzle contracts exist so later plans can implement data and UI against fixed types."
  artifacts:
    - path: "package.json"
      provides: "React, Vite, Vitest, and Testing Library scripts and dependencies"
      contains: "\"test\": \"vitest\""
    - path: "src/test/app-shell.smoke.test.tsx"
      provides: "Wave 0 smoke coverage proving the test harness actually runs"
      contains: "App"
    - path: "src/types/puzzle.ts"
      provides: "Shared puzzle and starter-pack contracts"
      exports: ["Difficulty", "PuzzleClue", "PuzzleRecord", "StarterPackRecord"]
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script invokes the configured Vitest runner"
      pattern: "vitest"
    - from: "src/test/app-shell.smoke.test.tsx"
      to: "src/App.tsx"
      via: "render smoke test through Testing Library"
      pattern: "from ['\\\"]@?/App['\\\"]|from ['\\\"]\\.\\./App['\\\"]"
---

<objective>
Create the true Wave 0 foundation for Phase 1: a runnable React + TypeScript app, shared puzzle contracts, and a real smoke test that makes the first `npm test -- --run` meaningful.

Purpose: The checker identified that Phase 1 assumed passing tests before any test file existed. This plan fixes that by making the first verification run prove the harness works.
Output: Buildable frontend scaffold, shared puzzle contracts, placeholder shell, and one smoke test that passes immediately.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md
@.planning/phases/01-starter-pack-board-shell/01-RESEARCH.md
@.planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md
@Origianl Assets/PROJECT.md
@Origianl Assets/REQUIREMENTS.md
@Origianl Assets/squares-starter-pack.json

<interfaces>
Create these exact contracts in `src/types/puzzle.ts` so later plans can build against them without redefining shape:

```ts
export type Difficulty = "easy" | "medium" | "hard";

export interface PuzzleClue {
  row: number;
  col: number;
  value: number;
}

export interface PuzzleSolutionRectangle {
  row: number;
  col: number;
  width: number;
  height: number;
  area: number;
}

export interface PuzzleRecord {
  id: string;
  size: number;
  difficulty: Difficulty;
  clues: PuzzleClue[];
  solution: PuzzleSolutionRectangle[];
}

export interface StarterPackCounts {
  total: number;
  easy: number;
  medium: number;
  hard: number;
}

export interface StarterPackRecord {
  game: "Squares";
  version: string;
  format: "starter-pack";
  notes: string[];
  counts: StarterPackCounts;
  puzzles: PuzzleRecord[];
}

export interface PuzzleListItem extends PuzzleRecord {
  packIndex: number;
  packTotal: number;
  difficultyLabel: "Easy" | "Medium" | "Hard";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the frontend, placeholder shell, and Wave 0 test harness</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, vite.config.ts, vitest.config.ts, index.html, src/main.tsx, src/App.tsx, src/test/app-shell.smoke.test.tsx, src/vite-env.d.ts, src/styles/tokens.css, src/styles/global.css, src/test/setup.ts</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-starter-pack-board-shell/01-CONTEXT.md, .planning/phases/01-starter-pack-board-shell/01-RESEARCH.md, package.json, src/App.tsx</read_first>
  <action>Create a Vite React TypeScript app in-place instead of a secondary subdirectory. In `package.json`, add scripts `dev`, `build`, `preview`, and `test` where `test` runs `vitest --run` or an equivalent non-watch Vitest command so the first verification run succeeds in CI-style mode. Add runtime deps `react` and `react-dom`. Add dev deps `typescript`, `vite`, `@vitejs/plugin-react`, `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom`. Configure `tsconfig.json` with `resolveJsonModule: true`, `jsx: "react-jsx"`, and a path alias `@/* -> src/*`. Create `vitest.config.ts` with `environment: "jsdom"` and `setupFiles: ["./src/test/setup.ts"]`. In `src/main.tsx`, import `src/styles/tokens.css` and `src/styles/global.css` before rendering `App`. Keep `src/App.tsx` as a read-only placeholder shell with no drag state, no puzzle validation state, and no placement logic per D-01 through D-03 and the Phase 1 boundary. Add `src/test/app-shell.smoke.test.tsx` as the explicit Wave 0 smoke test that renders the placeholder app shell and proves the harness is real before any feature-specific tests exist.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>`package.json` contains a `test` script with a non-watch Vitest invocation; `vitest.config.ts` contains `environment: "jsdom"`; `src/main.tsx` imports both `./styles/tokens.css` and `./styles/global.css`; `src/test/app-shell.smoke.test.tsx` exists and renders `App`; `npm test -- --run` exits successfully because at least one real test file exists.</acceptance_criteria>
  <done>The repo contains a runnable frontend scaffold and a real Wave 0 smoke test that makes the first fast test command trustworthy.</done>
</task>

<task type="auto">
  <name>Task 2: Define the shared puzzle contracts for downstream plans</name>
  <files>src/types/puzzle.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-starter-pack-board-shell/01-CONTEXT.md, .planning/phases/01-starter-pack-board-shell/01-RESEARCH.md, Origianl Assets/squares-starter-pack.json, src/types/puzzle.ts, src/data/starterPack.ts</read_first>
  <action>Implement the interfaces shown in the plan context inside `src/types/puzzle.ts`. Keep this file contract-only: exported types and interfaces for difficulties, clues, puzzle records, starter-pack records, and normalized list items. Do not create the JSON adapter here; that moves to the next plan so Wave 1 stays under the file-count threshold and the adapter can be verified against a real test base. The purpose of this step is to lock the data shape before implementation so later plans can import types instead of redefining them.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>`src/types/puzzle.ts` exports `PuzzleRecord`, `StarterPackRecord`, and `PuzzleListItem`; no feature module defines duplicate puzzle contracts; `npm test -- --run` still passes after the type file is added.</acceptance_criteria>
  <done>Phase 1 has stable type contracts and a real smoke-tested app foundation for the adapter and UI plans that follow.</done>
</task>

</tasks>

<verification>
Run `npm test -- --run` after each task. Plan success requires the app scaffold to compile for tests and the Wave 0 smoke test to pass before any feature-specific test plan begins.
</verification>

<success_criteria>
Phase 1 has a stable technical base when:
- The repo contains a working Vite + React + TypeScript + Vitest setup.
- `src/test/app-shell.smoke.test.tsx` gives the repo a real passing test before board or data features are implemented.
- `src/types/puzzle.ts` defines the shared contracts that later plans use for data and UI work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-starter-pack-board-shell/01-01-SUMMARY.md`
</output>
