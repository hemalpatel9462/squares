---
phase: 01-starter-pack-board-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/data/starterPack.ts
  - src/data/__tests__/starterPack.test.ts
autonomous: true
requirements:
  - PACK-01
  - PACK-02
must_haves:
  truths:
    - "The shipped starter-pack JSON loads through a typed adapter instead of raw UI reads."
    - "The bundled content exposes all 40 handcrafted puzzles with verified counts and legal board sizes."
    - "Downstream UI can access pack items by index without knowing the raw JSON format."
  artifacts:
    - path: "src/data/starterPack.ts"
      provides: "Canonical starter-pack loader and normalization helpers"
      exports: ["starterPack", "starterPackMeta", "starterPackPuzzles", "getPuzzleById", "getPuzzleByIndex"]
    - path: "src/data/__tests__/starterPack.test.ts"
      provides: "Automated validation for pack integrity"
      contains: "40"
  key_links:
    - from: "src/data/starterPack.ts"
      to: "Origianl Assets/squares-starter-pack.json"
      via: "typed JSON import"
      pattern: "squares-starter-pack\\.json"
    - from: "src/data/__tests__/starterPack.test.ts"
      to: "src/data/starterPack.ts"
      via: "Vitest assertions against normalized exports"
      pattern: "from ['\\\"]../starterPack['\\\"]"
---

<objective>
Implement the canonical typed starter-pack adapter and lock it with automated tests.

Purpose: The checker required the oversized Wave 1 plan to be split. This plan isolates the data adapter and integrity tests so the board UI can depend on a stable, tested content layer.
Output: Normalized starter-pack loader plus automated pack-integrity tests.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md
@.planning/phases/01-starter-pack-board-shell/01-RESEARCH.md
@.planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md
@.planning/phases/01-starter-pack-board-shell/01-01-PLAN.md
@Origianl Assets/squares-starter-pack.json

<interfaces>
Build against the Plan 01 contracts:

```ts
import type {
  Difficulty,
  PuzzleListItem,
  PuzzleRecord,
  StarterPackRecord,
} from "@/types/puzzle";
```

`src/data/starterPack.ts` should normalize the raw JSON into `StarterPackRecord` and `PuzzleListItem` exports so downstream UI plans can consume typed pack data without importing the JSON directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed starter-pack adapter</name>
  <files>src/data/starterPack.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-starter-pack-board-shell/01-CONTEXT.md, .planning/phases/01-starter-pack-board-shell/01-RESEARCH.md, Origianl Assets/squares-starter-pack.json, src/types/puzzle.ts, src/data/starterPack.ts</read_first>
  <action>Implement the canonical adapter in `src/data/starterPack.ts`. Import `../../Origianl Assets/squares-starter-pack.json` as the content source for D-04, PACK-01, and PACK-02. Validate the raw object at module load time: `game === "Squares"`, `format === "starter-pack"`, `counts.total === 40`, `counts.easy === 14`, `counts.medium === 14`, `counts.hard === 12`, every puzzle `size` is between `4` and `8`, and every puzzle `difficulty` is one of `easy|medium|hard`. Export `starterPack`, `starterPackMeta`, `starterPackPuzzles`, `getPuzzleById(id)`, and `getPuzzleByIndex(index)` where each puzzle item includes `packIndex`, `packTotal`, and capitalized `difficultyLabel`. Do not read the raw JSON directly from React components.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>`src/data/starterPack.ts` contains the string `squares-starter-pack.json`; `src/data/starterPack.ts` contains explicit checks for `40`, `14`, `14`, and `12`; `src/data/starterPack.ts` exports both `getPuzzleById` and `getPuzzleByIndex`; `npm test -- --run` still passes after the adapter is added.</acceptance_criteria>
  <done>The app has one canonical, typed, validated starter-pack module that downstream UI can consume without knowing the raw JSON structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the content contract with automated tests</name>
  <files>src/data/__tests__/starterPack.test.ts</files>
  <read_first>.planning/phases/01-starter-pack-board-shell/01-VALIDATION.md, Origianl Assets/squares-starter-pack.json, src/types/puzzle.ts, src/data/starterPack.ts, src/data/__tests__/starterPack.test.ts</read_first>
  <behavior>
    - Test 1: the normalized starter pack exposes exactly 40 puzzles and preserves the 14/14/12 difficulty counts.
    - Test 2: representative pack entries include valid ids, clue coordinates inside bounds, and board sizes from 4 through 8 only.
    - Test 3: `getPuzzleByIndex(0)` returns the first shipped puzzle and `getPuzzleByIndex(39)` returns the last shipped puzzle.
  </behavior>
  <action>Write Vitest coverage in `src/data/__tests__/starterPack.test.ts` before adjusting adapter details. Assert exact counts, legal size range, and valid helper behavior for first and last puzzle access so PACK-02 is guarded by tests from the start. Keep the smoke test from Plan 01 green; this plan should add feature verification, not replace the Wave 0 harness.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <acceptance_criteria>`src/data/__tests__/starterPack.test.ts` contains assertions for `40`, `14`, `getPuzzleByIndex(0)`, and `getPuzzleByIndex(39)`; `npm test -- --run` passes.</acceptance_criteria>
  <done>The starter-pack content contract is covered by fast automated tests and safe for UI wiring in later plans.</done>
</task>

</tasks>

<verification>
Run `npm test -- --run` after each task. This plan is complete only when the adapter imports the real starter pack and tests prove the repo still ships the exact 40 bundled puzzles.
</verification>

<success_criteria>
Phase 1 content loading is on track when:
- `src/data/starterPack.ts` is the only module that imports the starter-pack JSON.
- Automated tests verify the exact shipped puzzle counts and index access helpers.
- Later UI plans can consume normalized puzzle data without knowing the raw JSON structure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-starter-pack-board-shell/01-02-SUMMARY.md`
</output>
