---
phase: 01-starter-pack-board-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/PuzzleBoard.tsx
  - src/components/PuzzleMeta.tsx
  - src/components/PuzzleShell.tsx
  - src/components/__tests__/PuzzleBoard.test.tsx
  - src/App.tsx
autonomous: true
requirements:
  - BOARD-01
  - BOARD-02
must_haves:
  truths:
    - "A player can see a square board for puzzles from 4x4 through 8x8."
    - "Clue numbers appear in the correct cells and are visually distinct from empty cells."
    - "The board screen is read-only and does not imply placement interaction yet."
  artifacts:
    - path: "src/components/PuzzleBoard.tsx"
      provides: "Read-only board grid rendering"
      exports: ["PuzzleBoard"]
    - path: "src/components/PuzzleShell.tsx"
      provides: "Integrated panel for metadata and board"
      exports: ["PuzzleShell"]
    - path: "src/components/__tests__/PuzzleBoard.test.tsx"
      provides: "Automated render coverage across representative board sizes"
      contains: "4"
  key_links:
    - from: "src/App.tsx"
      to: "src/data/starterPack.ts"
      via: "initial puzzle selection from normalized pack data"
      pattern: "starterPackPuzzles|getPuzzleByIndex"
    - from: "src/components/PuzzleShell.tsx"
      to: "src/components/PuzzleBoard.tsx"
      via: "board prop wiring"
      pattern: "<PuzzleBoard"
---

<objective>
Implement the read-only puzzle screen: integrated metadata plus board renderer for the current puzzle using the typed starter-pack data from Plan 02.

Purpose: This is the first user-visible slice of Phase 1 and covers the core board shell without crossing into placement or rules work.
Output: Puzzle screen components, board render tests, and app wiring for the initial puzzle view.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md
@.planning/phases/01-starter-pack-board-shell/01-RESEARCH.md
@.planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md
@.planning/phases/01-starter-pack-board-shell/01-02-PLAN.md
@Origianl Assets/squares-starter-pack.json

<interfaces>
Build against the Plan 01 contracts and Plan 02 helper exports:

```ts
import type { PuzzleListItem } from "@/types/puzzle";
import { getPuzzleByIndex, starterPackPuzzles } from "@/data/starterPack";
```

`PuzzleBoard` should accept one `puzzle: PuzzleListItem` prop and render cells by puzzle size and clue coordinates. `PuzzleShell` should accept one `puzzle: PuzzleListItem` prop and compose metadata plus board in a single framed surface per D-02 and D-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the read-only board renderer</name>
  <files>src/components/PuzzleBoard.tsx, src/components/PuzzleMeta.tsx, src/components/PuzzleShell.tsx</files>
  <read_first>.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md, .planning/phases/01-starter-pack-board-shell/01-RESEARCH.md, .planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md, src/types/puzzle.ts, src/data/starterPack.ts, src/components/PuzzleBoard.tsx, src/components/PuzzleMeta.tsx, src/components/PuzzleShell.tsx</read_first>
  <action>Create a presentational component set only. In `src/components/PuzzleBoard.tsx`, render an `N x N` grid from `puzzle.size` using an actual CSS grid and a deterministic row-major cell map. For each cell, add `data-row`, `data-col`, and `data-cell-kind="clue"` or `data-cell-kind="empty"`. Render clue values centered only for matching coordinates. In `src/components/PuzzleMeta.tsx`, show exactly `Puzzle {puzzle.id}`, the capitalized difficulty label, and `Puzzle {puzzle.packIndex + 1} of {puzzle.packTotal}` per D-04. In `src/components/PuzzleShell.tsx`, place metadata and board in one surface so the screen reads as a single panel per D-05. Keep the board static: no pointer handlers, no `button` cells, no focusable grid cells, no preview overlays, and no solve status.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>The app has a read-only board and integrated metadata shell that matches the phase boundary and Phase 1 decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the initial puzzle screen and prove board rendering with tests</name>
  <files>src/components/__tests__/PuzzleBoard.test.tsx, src/App.tsx</files>
  <read_first>.planning/phases/01-starter-pack-board-shell/01-VALIDATION.md, src/data/starterPack.ts, src/components/PuzzleBoard.tsx, src/components/PuzzleShell.tsx, src/components/__tests__/PuzzleBoard.test.tsx, src/App.tsx</read_first>
  <behavior>
    - Test 1: representative 4x4, 6x6, and 8x8 puzzles render the correct number of cells.
    - Test 2: clue cells show the expected clue value while empty cells remain text-empty.
    - Test 3: the app shell loads the first starter-pack puzzle and shows integrated metadata with the board.
  </behavior>
  <action>Write the render tests first in `src/components/__tests__/PuzzleBoard.test.tsx`. Then update `src/App.tsx` to read the initial puzzle from `getPuzzleByIndex(0)` and render `PuzzleShell`. Keep the app state simple: a single current puzzle object only, no router, no drag selection, and no persistence. Ensure representative board sizes from the shipped data are covered so BOARD-01 and BOARD-02 are enforced automatically.</action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>The initial app screen shows a real starter-pack puzzle with verified board rendering across representative sizes.</done>
</task>

</tasks>

<verification>
Run `npm test -- --run` after each task. This plan is complete only when the app renders the first real puzzle and the board tests prove correct cell and clue placement behavior.
</verification>

<success_criteria>
Phase 1 board-shell implementation is on track when:
- The app renders a square read-only grid from starter-pack data.
- Clue cells are distinguishable in code and in test assertions.
- The current puzzle metadata is visible within the same shell as the board.
</success_criteria>

<output>
After completion, create `.planning/phases/01-starter-pack-board-shell/01-03-SUMMARY.md`
</output>
