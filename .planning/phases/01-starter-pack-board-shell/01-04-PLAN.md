---
phase: 01-starter-pack-board-shell
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/components/PuzzleNavigator.tsx
  - src/components/PuzzleShell.tsx
  - src/components/__tests__/App.test.tsx
  - src/App.tsx
  - src/styles/tokens.css
  - src/styles/global.css
autonomous: false
requirements:
  - BOARD-03
must_haves:
  truths:
    - "A player can see puzzle id, difficulty, and pack progress while playing."
    - "A player can move through the full shipped starter pack from within the board shell."
    - "Desktop and mobile both preserve the board as the focal point inside a premium framed surface."
  artifacts:
    - path: "src/components/PuzzleNavigator.tsx"
      provides: "Previous and Next Puzzle controls"
      exports: ["PuzzleNavigator"]
    - path: "src/styles/tokens.css"
      provides: "Phase 1 spacing, typography, color, and radius tokens"
      contains: "--color-dominant"
    - path: "src/components/__tests__/App.test.tsx"
      provides: "Automated navigation and metadata wiring coverage"
      contains: "Next Puzzle"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/PuzzleNavigator.tsx"
      via: "current puzzle index state and navigation handlers"
      pattern: "setCurrentPuzzleIndex|handleNextPuzzle|handlePreviousPuzzle"
    - from: "src/styles/tokens.css"
      to: "src/styles/global.css"
      via: "CSS custom properties consumed by the shell and page layout"
      pattern: "--color-dominant|--space-md|--radius-shell"
---

<objective>
Finish the Phase 1 experience with in-shell puzzle navigation, the premium responsive visual system, and a final human visual verification pass.

Purpose: Phase 1 is not complete with a raw prototype board. The screen has to communicate context clearly and feel intentionally framed on both desktop and mobile.
Output: Responsive premium styling, previous/next puzzle access, app navigation tests, and a human verification gate.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md
@.planning/phases/01-starter-pack-board-shell/01-RESEARCH.md
@.planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md
@.planning/phases/01-starter-pack-board-shell/01-VALIDATION.md
@.planning/phases/01-starter-pack-board-shell/01-03-PLAN.md

<interfaces>
Continue using `PuzzleListItem` from `src/types/puzzle.ts`.

`PuzzleNavigator` props:

```ts
interface PuzzleNavigatorProps {
  canGoPrevious: boolean;
  canGoNext: boolean;
  onPrevious: () => void;
  onNext: () => void;
}
```

`App` should own a numeric `currentPuzzleIndex` state and derive the displayed puzzle via `getPuzzleByIndex(currentPuzzleIndex)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add full pack navigation and the premium responsive visual system</name>
  <files>src/components/PuzzleNavigator.tsx, src/components/PuzzleShell.tsx, src/components/__tests__/App.test.tsx, src/App.tsx, src/styles/tokens.css, src/styles/global.css</files>
  <read_first>.planning/phases/01-starter-pack-board-shell/01-CONTEXT.md, .planning/phases/01-starter-pack-board-shell/01-UI-SPEC.md, src/data/starterPack.ts, src/components/PuzzleShell.tsx, src/App.tsx, src/styles/tokens.css, src/styles/global.css, src/components/__tests__/App.test.tsx</read_first>
  <behavior>
    - Test 1: `Next Puzzle` advances the rendered puzzle id and updates `Puzzle {index} of 40`.
    - Test 2: `Previous` is disabled on the first puzzle and `Next Puzzle` is disabled on the fortieth puzzle.
    - Test 3: the shell renders the metadata band and board inside one framed surface while preserving the exact token values from the UI spec.
  </behavior>
  <action>Write navigation-focused app tests first in `src/components/__tests__/App.test.tsx`. Then add `currentPuzzleIndex` state to `src/App.tsx` and wire `Previous` / `Next Puzzle` controls through `src/components/PuzzleNavigator.tsx`. Use `getPuzzleByIndex` so all 40 puzzles remain accessible in order. Update `src/components/PuzzleShell.tsx`, `src/styles/tokens.css`, and `src/styles/global.css` to implement the exact Phase 1 design contract values: colors `#F3EEE4`, `#D9E0DC`, `#C56D3D`, `#B44C3C`; background radial blend `#F7F3EB` to `#E7DED0`; spacing tokens `4, 8, 16, 24, 32, 48, 64`; typography sizes `14, 16, 20, 28` with weights `400` and `600`; shell radius `20px`; chip radius `12px`; desktop max width `1200px` with outer padding `32px`; mobile outer padding `16px`; board width rules `min(72vh, 640px)` on desktop and `min(calc(100vw - 32px), 520px)` on mobile. Keep the board non-interactive and integrate the metadata and navigator into the same surface per D-01 through D-08.</action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <done>The screen exposes the full starter pack through in-shell navigation and matches the approved premium responsive direction in code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the board shell composition on desktop and mobile widths</name>
  <files>src/App.tsx, src/components/PuzzleShell.tsx, src/components/PuzzleNavigator.tsx, src/styles/tokens.css, src/styles/global.css</files>
  <what-built>Read-only starter-pack board shell with integrated metadata, previous/next navigation, and premium responsive styling.</what-built>
  <action>Start the app locally, open the puzzle screen, and inspect at a desktop width around `1440px` and a mobile width around `390px`. Verify the board remains the focal element, metadata stays inside the same framed surface, clue cells are obviously distinct from empty cells, and the board does not feel cramped on mobile. Confirm puzzle navigation updates the board in place without changing the overall shell composition.</action>
  <verify>
    <automated>npm test -- --run && npm run build</automated>
  </verify>
  <how-to-verify>
    1. Run `npm install` if dependencies are missing, then `npm run dev`.
    2. Open the local URL in a desktop-width browser window and confirm the shell reads as one premium panel with the board as the hero.
    3. Narrow the viewport to a mobile width near `390px` and confirm the board still dominates the surface and metadata compresses without crowding it.
    4. Click `Next Puzzle` and `Previous` and confirm only the puzzle contents change while the shell framing stays stable.
  </how-to-verify>
  <resume-signal>Type `approved` if the shell looks right, or describe the visual issues that need correction.</resume-signal>
  <done>The human reviewer explicitly approves the desktop and mobile shell composition or returns actionable visual issues for correction.</done>
</task>

</tasks>

<verification>
Run `npm test -- --run && npm run build` before the checkpoint. The plan is complete only after automated checks pass and the human reviewer approves the desktop and mobile presentation.
</verification>

<success_criteria>
Phase 1 is complete when:
- The current puzzle id, difficulty, and pack progress are always visible in the shell.
- The player can reach every shipped puzzle through `Previous` and `Next Puzzle`.
- The board shell feels intentionally framed and board-first on both desktop and mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/01-starter-pack-board-shell/01-04-SUMMARY.md`
</output>
