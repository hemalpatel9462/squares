---
phase: 02-deterministic-rectangle-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/rules.ts
  - src/rules/index.ts
  - src/rules/__tests__/ruleTestUtils.ts
  - src/rules/__tests__/rectangleRules.test.ts
  - src/rules/__tests__/boardRules.test.ts
  - src/rules/__tests__/starterPackSolutions.test.ts
autonomous: true
requirements:
  - RULE-07
must_haves:
  truths:
    - "The rules engine exposes explicit, UI-independent contracts for candidate placements and deterministic diagnostics."
    - "Phase 2 has executable test files that define the required invalid-placement and solved-board behaviors before engine implementation expands."
    - "Shipped starter-pack solution rectangles are treated as verification fixtures, not as gameplay authority."
  artifacts:
    - path: "src/types/rules.ts"
      provides: "Pure rules-engine contracts and deterministic issue enums"
      exports: ["CandidatePlacement", "PlacementIssue", "BoardAnalysis", "PlacementAnalysis"]
    - path: "src/rules/index.ts"
      provides: "Stable export surface for later UI and engine consumers"
      exports: ["analyzePlacement", "getPrimaryPlacementIssue", "analyzeBoard", "isBoardSolved"]
    - path: "src/rules/__tests__/rectangleRules.test.ts"
      provides: "Placement diagnostics contract coverage"
      contains: "rejects rectangles missing origin clue"
    - path: "src/rules/__tests__/boardRules.test.ts"
      provides: "Solved-board and coverage contract coverage"
      contains: "marks a fully covered valid board as solved"
    - path: "src/rules/__tests__/starterPackSolutions.test.ts"
      provides: "Shipped fixture verification without making solutions authoritative"
      contains: "accepts shipped starter-pack solution rectangles as solved partitions"
  key_links:
    - from: "src/rules/index.ts"
      to: "src/types/rules.ts"
      via: "named type and function exports"
      pattern: "from \"@/types/rules\"|from \"./"
    - from: "src/rules/__tests__/rectangleRules.test.ts"
      to: "src/rules/index.ts"
      via: "public rules API imports"
      pattern: "from \"@/rules\""
    - from: "src/rules/__tests__/starterPackSolutions.test.ts"
      to: "src/data/starterPack.ts"
      via: "starter-pack fixture imports"
      pattern: "from \"@/data/starterPack\""
---

<objective>
Lock the Phase 2 rules-engine contracts and test scaffolding before implementation work begins.

Purpose: Enforce D-04 through D-13 with an explicit public API, deterministic failure ordering, and executable tests that keep the logic UI-independent.
Output: Contract files, shared rule-test helpers, and failing-or-pending test coverage for placement diagnostics, board analysis, and starter-pack fixture verification.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md
@.planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md
@.planning/phases/01-starter-pack-board-shell/01-02-SUMMARY.md
@src/types/puzzle.ts
@src/data/starterPack.ts

<interfaces>
From src/types/puzzle.ts:
```typescript
export interface PuzzleClue {
  row: number;
  col: number;
  value: number;
}

export interface PuzzleSolutionRectangle {
  row: number;
  col: number;
  width: number;
  height: number;
  area: number;
}

export interface PuzzleRecord {
  id: string;
  size: number;
  difficulty: Difficulty;
  clues: PuzzleClue[];
  solution: PuzzleSolutionRectangle[];
}

export interface PuzzleListItem extends PuzzleRecord {
  packIndex: number;
  packTotal: number;
  difficultyLabel: "Easy" | "Medium" | "Hard";
}
```

From src/data/starterPack.ts:
```typescript
export const starterPackPuzzles: PuzzleListItem[];
export function getPuzzleById(id: string): PuzzleListItem | undefined;
export function getPuzzleByIndex(index: number): PuzzleListItem | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the public rules contracts and export surface</name>
  <files>src/types/rules.ts, src/rules/index.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/types/puzzle.ts, src/rules/index.ts</read_first>
  <action>Create `src/types/rules.ts` with the pure engine contracts required by D-04 through D-13: `CellCoord`, `RectangleBounds`, `CandidatePlacement`, `PlacementIssue`, `PlacementAnalysis`, `BoardIssue`, `BoardAnalysis`, and any small supporting metadata types. Hard-code the primary issue priority array in this contract layer as `["missing_origin_clue", "contains_other_clue", "wrong_area", "out_of_bounds", "overlap"]` per D-10. Create `src/rules/index.ts` that exports the future public API names `analyzePlacement`, `getPrimaryPlacementIssue`, `isPlacementValid`, `analyzeBoard`, and `isBoardSolved` plus the contract types, even if some exports are temporary stubs that throw until later plans implement them. Keep every type and export UI-agnostic and derived from explicit origin-clue input per D-07 and D-08.</action>
  <acceptance_criteria>`rg -n 'export type PlacementIssue = .*missing_origin_clue.*contains_other_clue.*wrong_area.*out_of_bounds.*overlap' src/types/rules.ts` returns the deterministic issue union. `rg -n 'export const PLACEMENT_ISSUE_PRIORITY = \\[' src/types/rules.ts` returns the priority constant. `rg -n 'export \\{ analyzePlacement, getPrimaryPlacementIssue, isPlacementValid, analyzeBoard, isBoardSolved \\}' src/rules/index.ts` returns the public export surface.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>`src/types/rules.ts` exists with the locked rules-first contracts, and `src/rules/index.ts` exposes the stable API names future plans will implement.</done>
</task>

<task type="auto">
  <name>Task 2: Add rule-test helpers and contract coverage for placement and board analysis</name>
  <files>src/rules/__tests__/ruleTestUtils.ts, src/rules/__tests__/rectangleRules.test.ts, src/rules/__tests__/boardRules.test.ts, src/rules/__tests__/starterPackSolutions.test.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/data/__tests__/starterPack.test.ts, src/data/starterPack.ts, src/types/rules.ts</read_first>
  <action>Create `src/rules/__tests__/ruleTestUtils.ts` with compact synthetic puzzle builders and rectangle-placement helpers so the later engine tests do not duplicate fixture wiring. Then add three Vitest files that pin the required behaviors with exact case names from research: `rectangleRules.test.ts` must cover `rejects rectangles missing origin clue`, `rejects rectangles containing non-origin clues`, `rejects rectangles with wrong area`, `rejects out-of-bounds rectangles`, `rejects overlaps against placed rectangles`, and primary-issue ordering when multiple failures apply. `boardRules.test.ts` must cover `marks a fully covered valid board as solved` plus at least one uncovered-cell and invalid-placement propagation case. `starterPackSolutions.test.ts` must iterate shipped starter-pack puzzles and assert that bundled `solution` partitions pass `analyzeBoard` as fixtures without comparing for identity or treating them as the only valid answer, preserving D-01 through D-03.</action>
  <acceptance_criteria>`rg -n 'rejects rectangles missing origin clue|rejects rectangles containing non-origin clues|rejects rectangles with wrong area|rejects out-of-bounds rectangles|rejects overlaps against placed rectangles' src/rules/__tests__/rectangleRules.test.ts` matches all five invalid-placement cases. `rg -n 'marks a fully covered valid board as solved' src/rules/__tests__/boardRules.test.ts` matches the solved-board contract. `rg -n 'accepts shipped starter-pack solution rectangles as solved partitions' src/rules/__tests__/starterPackSolutions.test.ts` matches the fixture-verification case. `rg -n 'from \"@/rules\"' src/rules/__tests__/rectangleRules.test.ts src/rules/__tests__/boardRules.test.ts src/rules/__tests__/starterPackSolutions.test.ts` shows tests import the public rules API rather than private internals.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/rules/__tests__/rectangleRules.test.ts src/rules/__tests__/boardRules.test.ts src/rules/__tests__/starterPackSolutions.test.ts</automated>
  </verify>
  <done>Phase 2 has reusable test fixtures and explicit contract coverage that can drive later implementation without React or UI setup.</done>
</task>

</tasks>

<verification>
The public rules module exists, exported names are stable, and the Phase 2 tests define the full deterministic contract for placement failures, board solving, and shipped-fixture verification.
</verification>

<success_criteria>
Executors can start Phase 2 implementation by importing a stable rules contract file and running focused Vitest suites that already encode the required rules-first behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-rectangle-rules/02-01-SUMMARY.md`
</output>
