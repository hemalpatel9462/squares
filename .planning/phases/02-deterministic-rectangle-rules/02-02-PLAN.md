---
phase: 02-deterministic-rectangle-rules
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/rules/placementAnalysis.ts
  - src/rules/index.ts
  - src/rules/__tests__/rectangleRules.test.ts
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-07
must_haves:
  truths:
    - "A candidate rectangle with explicit origin-clue coordinates produces the same placement analysis every time for the same inputs."
    - "Invalid placements accumulate every applicable failure, while the gameplay-facing helper exposes one deterministic primary reason."
    - "Placement analysis stays pure TypeScript and does not depend on React components, DOM state, or bundled solution identity."
  artifacts:
    - path: "src/rules/placementAnalysis.ts"
      provides: "Full placement diagnostics and deterministic primary-reason projection"
      exports: ["analyzePlacement", "getPrimaryPlacementIssue", "isPlacementValid"]
    - path: "src/rules/__tests__/rectangleRules.test.ts"
      provides: "Proof of all invalid-placement branches and ordering"
      contains: "returns the deterministic primary issue when multiple failures apply"
  key_links:
    - from: "src/rules/placementAnalysis.ts"
      to: "src/types/rules.ts"
      via: "shared issue enums and analysis result contracts"
      pattern: "from \"@/types/rules\""
    - from: "src/rules/index.ts"
      to: "src/rules/placementAnalysis.ts"
      via: "public re-export"
      pattern: "from \"./placementAnalysis\""
    - from: "src/rules/__tests__/rectangleRules.test.ts"
      to: "src/rules/placementAnalysis.ts"
      via: "public rules exports"
      pattern: "analyzePlacement|getPrimaryPlacementIssue|isPlacementValid"
---

<objective>
Implement the pure placement-analysis engine and deterministic gameplay wrapper for invalid rectangles.

Purpose: Satisfy RULE-01 through RULE-05 with one rules-first analyzer that produces full diagnostics and a fixed primary issue order for later UI consumption.
Output: A reusable placement-analysis module, public exports, and passing unit coverage for all invalid-placement rules and wrapper behavior.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md
@.planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md
@.planning/phases/02-deterministic-rectangle-rules/02-01-PLAN.md
@src/types/rules.ts
@src/types/puzzle.ts
@src/rules/__tests__/rectangleRules.test.ts

<interfaces>
The plan must preserve these public names from Plan 02-01:
```typescript
export interface CandidatePlacement {
  origin: { row: number; col: number };
  rectangle: { row: number; col: number; width: number; height: number };
}

export type PlacementIssue =
  | "missing_origin_clue"
  | "contains_other_clue"
  | "wrong_area"
  | "out_of_bounds"
  | "overlap";

export function analyzePlacement(...args: unknown[]): PlacementAnalysis;
export function getPrimaryPlacementIssue(issues: PlacementIssue[]): PlacementIssue | null;
export function isPlacementValid(...args: unknown[]): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish placement-analysis test coverage against the public API</name>
  <files>src/rules/__tests__/rectangleRules.test.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/rules/__tests__/rectangleRules.test.ts, src/types/rules.ts, src/rules/index.ts</read_first>
  <behavior>
    - Test 1: A rectangle that excludes the selected clue returns `issues` containing `missing_origin_clue` and `isValid === false`.
    - Test 2: A rectangle that includes a foreign clue returns `contains_other_clue` while preserving the explicit origin-clue contract from D-07 and D-08.
    - Test 3: A rectangle with wrong area, out-of-bounds geometry, and overlap can report multiple issues, but `getPrimaryPlacementIssue` returns the fixed D-10 ordering result.
  </behavior>
  <action>Complete `src/rules/__tests__/rectangleRules.test.ts` so it asserts the exact `PlacementAnalysis` fields the engine must return: deterministic `issues` arrays, `primaryIssue`, `coveredCells` or equivalent normalized geometry metadata, and boolean validity derived from those issues. Keep every test importing from `@/rules` rather than private files. Use table-driven `it.each` cases for the D-10 priority order and explicit synthetic placements from `ruleTestUtils.ts` so the failing contract is compact and repeatable.</action>
  <acceptance_criteria>`rg -n 'it.each\\(' src/rules/__tests__/rectangleRules.test.ts` confirms table-driven ordering coverage. `rg -n 'missing_origin_clue|contains_other_clue|wrong_area|out_of_bounds|overlap' src/rules/__tests__/rectangleRules.test.ts` shows all five required issue codes are asserted. `rg -n 'from \"@/rules\"' src/rules/__tests__/rectangleRules.test.ts` confirms the test targets the public API.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/rules/__tests__/rectangleRules.test.ts</automated>
  </verify>
  <done>The placement test suite fully describes the expected diagnostics and deterministic primary-reason behavior before implementation is finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement full placement analysis and deterministic gameplay wrappers</name>
  <files>src/rules/placementAnalysis.ts, src/rules/index.ts, src/rules/__tests__/rectangleRules.test.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/types/rules.ts, src/rules/index.ts, src/rules/__tests__/rectangleRules.test.ts, src/types/puzzle.ts</read_first>
  <behavior>
    - Test 1: `analyzePlacement` returns every applicable issue for a candidate placement instead of stopping at the first failure.
    - Test 2: `getPrimaryPlacementIssue` returns `missing_origin_clue`, then `contains_other_clue`, then `wrong_area`, then `out_of_bounds`, then `overlap` when multiple issues coexist.
    - Test 3: `isPlacementValid` returns `true` only when `analyzePlacement(...).issues` is empty.
  </behavior>
  <action>Implement `src/rules/placementAnalysis.ts` as a pure module with no React imports. Normalize rectangle cell coordinates into deterministic row-major cell keys, look up the explicit origin clue in the puzzle clue set, collect every applicable issue in the D-10 order, and return one `PlacementAnalysis` object that includes `issues`, `primaryIssue`, `isValid`, and normalized rectangle metadata. Detect overlap from a passed-in placed-rectangle list using the same cell-key representation the board analyzer can reuse later. Re-export the implemented functions from `src/rules/index.ts`, and update any test assertions that need the final exact field names while preserving the contract semantics from Task 1.</action>
  <acceptance_criteria>`rg -n 'export function analyzePlacement|export function getPrimaryPlacementIssue|export function isPlacementValid' src/rules/placementAnalysis.ts` returns all three implemented functions. `rg -n 'missing_origin_clue.*contains_other_clue.*wrong_area.*out_of_bounds.*overlap' src/rules/placementAnalysis.ts` shows the fixed ordering is encoded in implementation, not test-only. `rg -n 'from \"./placementAnalysis\"' src/rules/index.ts` confirms the public API now points at the implementation module.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/rules/__tests__/rectangleRules.test.ts</automated>
  </verify>
  <done>Placement analysis passes focused unit tests and provides deterministic, UI-independent diagnostics for every invalid-rectangle rule.</done>
</task>

</tasks>

<verification>
Focused placement tests pass, the placement analyzer is pure and public through `@/rules`, and no invalid-placement rule requires UI state or bundled-solution comparisons.
</verification>

<success_criteria>
Later UI work can call one stable placement API to get both full diagnostics and a deterministic primary invalid reason for the same candidate input.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-rectangle-rules/02-02-SUMMARY.md`
</output>
