---
phase: 02-deterministic-rectangle-rules
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/rules/boardAnalysis.ts
  - src/rules/index.ts
  - src/rules/__tests__/boardRules.test.ts
  - src/rules/__tests__/starterPackSolutions.test.ts
autonomous: true
requirements:
  - RULE-06
  - RULE-07
must_haves:
  truths:
    - "The engine can determine whether a full board state is solved by checking coverage, clue satisfaction, and invalid placements through one deterministic analysis path."
    - "A board can fail solved-state checks for uncovered cells, overlaps, or invalid clue assignments without consulting the bundled solution identity."
    - "All shipped starter-pack solution rectangles pass the rules-first board analyzer as a fixture sanity check."
  artifacts:
    - path: "src/rules/boardAnalysis.ts"
      provides: "Full board analysis and solved-state helpers"
      exports: ["analyzeBoard", "isBoardSolved"]
    - path: "src/rules/__tests__/boardRules.test.ts"
      provides: "Board coverage, invalid placement propagation, and solved-state assertions"
      contains: "reports uncovered cells when the board is not fully covered"
    - path: "src/rules/__tests__/starterPackSolutions.test.ts"
      provides: "Starter-pack solution fixture verification through the same board analyzer"
      contains: "accepts shipped starter-pack solution rectangles as solved partitions"
  key_links:
    - from: "src/rules/boardAnalysis.ts"
      to: "src/rules/placementAnalysis.ts"
      via: "shared placement diagnostics reused for aggregate board analysis"
      pattern: "analyzePlacement"
    - from: "src/rules/index.ts"
      to: "src/rules/boardAnalysis.ts"
      via: "public re-export"
      pattern: "from \"./boardAnalysis\""
    - from: "src/rules/__tests__/starterPackSolutions.test.ts"
      to: "src/data/starterPack.ts"
      via: "fixture puzzles imported from shipped starter pack"
      pattern: "starterPackPuzzles"
---

<objective>
Implement complete solved-board analysis and prove it against synthetic failures plus the shipped starter-pack solution partitions.

Purpose: Fulfill RULE-06 with one deterministic board analyzer that aggregates placement diagnostics, coverage, and clue satisfaction without ever using bundled solutions as gameplay truth.
Output: A pure board-analysis module, public solved-state helpers, and passing board/starter-pack verification suites.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md
@.planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md
@.planning/phases/02-deterministic-rectangle-rules/02-02-PLAN.md
@src/types/rules.ts
@src/rules/placementAnalysis.ts
@src/data/starterPack.ts
@src/rules/__tests__/boardRules.test.ts
@src/rules/__tests__/starterPackSolutions.test.ts

<interfaces>
From the prior plan's public surface:
```typescript
export function analyzePlacement(...args: unknown[]): PlacementAnalysis;

export interface BoardAnalysis {
  isSolved: boolean;
  issues: BoardIssue[];
  uncoveredCells: string[];
  invalidPlacements: PlacementAnalysis[];
}

export function analyzeBoard(...args: unknown[]): BoardAnalysis;
export function isBoardSolved(...args: unknown[]): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize board-analysis and starter-pack fixture tests through the public API</name>
  <files>src/rules/__tests__/boardRules.test.ts, src/rules/__tests__/starterPackSolutions.test.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/rules/__tests__/boardRules.test.ts, src/rules/__tests__/starterPackSolutions.test.ts, src/rules/placementAnalysis.ts, src/data/starterPack.ts, src/types/rules.ts</read_first>
  <behavior>
    - Test 1: A fully covered set of valid rectangles marks the board solved with no board issues.
    - Test 2: Missing coverage leaves `isSolved === false` and reports deterministic uncovered-cell diagnostics.
    - Test 3: Starter-pack bundled `solution` rectangles pass `analyzeBoard`, but the test never compares the player state to the bundled layout for equality.
  </behavior>
  <action>Complete the remaining board-focused tests so `boardRules.test.ts` asserts exact aggregate outputs for solved boards, uncovered-cell failures, and propagation of invalid placements from `analyzePlacement`. Update `starterPackSolutions.test.ts` to iterate `starterPackPuzzles` and convert each shipped `solution` rectangle into the explicit candidate-placement input shape required by the engine, using the rectangle’s real enclosed clue as `origin`. Keep the assertions rules-first: check `analyzeBoard(...).isSolved === true` and empty board issues, not equality to the JSON solution list, preserving D-01 through D-03.</action>
  <acceptance_criteria>`rg -n 'reports uncovered cells when the board is not fully covered|propagates invalid placement diagnostics into board analysis' src/rules/__tests__/boardRules.test.ts` matches the aggregate-failure cases. `rg -n 'starterPackPuzzles' src/rules/__tests__/starterPackSolutions.test.ts` confirms the shipped puzzles drive the fixture test. `rg -n 'isSolved' src/rules/__tests__/starterPackSolutions.test.ts` confirms the assertion is rules-first solved-state analysis rather than direct layout equality.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/rules/__tests__/boardRules.test.ts src/rules/__tests__/starterPackSolutions.test.ts</automated>
  </verify>
  <done>The board-level test suites fully define solved-state semantics, uncovered-cell behavior, and shipped-fixture validation through the public rules API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic board analysis and solved-state helpers</name>
  <files>src/rules/boardAnalysis.ts, src/rules/index.ts, src/rules/__tests__/boardRules.test.ts, src/rules/__tests__/starterPackSolutions.test.ts</files>
  <read_first>.planning/phases/02-deterministic-rectangle-rules/02-CONTEXT.md, .planning/phases/02-deterministic-rectangle-rules/02-RESEARCH.md, src/types/rules.ts, src/rules/placementAnalysis.ts, src/rules/index.ts, src/rules/__tests__/boardRules.test.ts, src/rules/__tests__/starterPackSolutions.test.ts</read_first>
  <behavior>
    - Test 1: `analyzeBoard` reuses placement analysis for each rectangle and marks the board solved only when every cell is covered exactly once and every placement is valid.
    - Test 2: `analyzeBoard` returns deterministic uncovered-cell and invalid-placement diagnostics that later UI layers can consume without extra inference.
    - Test 3: `isBoardSolved` is a thin boolean wrapper over `analyzeBoard`, not a second validation path.
  </behavior>
  <action>Implement `src/rules/boardAnalysis.ts` as the aggregate engine for RULE-06. Reuse `analyzePlacement` for every candidate placement, compute row-major cell coverage over the full board, detect uncovered cells and any aggregate board issues, and return a single `BoardAnalysis` result that includes per-placement diagnostics and solved-state booleans. Ensure the module treats bundled `solution` data only as an input fixture when tests call it; gameplay truth remains the rules result itself per D-01 and D-02. Re-export `analyzeBoard` and `isBoardSolved` from `src/rules/index.ts`, then run the focused board suites and the full repo test suite.</action>
  <acceptance_criteria>`rg -n 'export function analyzeBoard|export function isBoardSolved' src/rules/boardAnalysis.ts` returns both implemented board helpers. `rg -n 'analyzePlacement' src/rules/boardAnalysis.ts` confirms aggregate logic reuses the placement engine instead of duplicating rule checks. `rg -n 'from \"./boardAnalysis\"' src/rules/index.ts` confirms the public API exposes the board analyzer.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>The rules engine can deterministically decide when a board is solved, report why it is not solved, and validate shipped starter-pack solution partitions through the same pure analysis path.</done>
</task>

</tasks>

<verification>
The board analyzer reuses placement diagnostics, focused board tests pass, shipped starter-pack solutions are green under rules-first analysis, and the full repo test suite stays green.
</verification>

<success_criteria>
Phase 2 finishes with a deterministic, UI-independent engine that later interaction work can trust for both per-placement validation and full solved-board analysis.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-rectangle-rules/02-03-SUMMARY.md`
</output>
