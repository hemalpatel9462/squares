---
phase: 03-live-rectangle-placement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/play.ts
  - src/App.tsx
  - src/components/PuzzleShell.tsx
  - src/components/PuzzleBoard.tsx
  - src/components/__tests__/testGeometry.ts
  - src/components/__tests__/PuzzleBoard.interaction.test.tsx
  - src/components/__tests__/App.placement.test.tsx
autonomous: true
requirements:
  - PLAY-04
must_haves:
  truths:
    - "Before the first successful placement, the player sees the explicit empty-board prompt that teaches how to start rectangle placement."
    - "Committed rectangles stay associated with the active puzzle so later live placement and navigation can build on puzzle-scoped state."
    - "The board receives committed placements plus a commit callback through typed props, making it ready to render live placement results without duplicating rules logic."
  artifacts:
    - path: "src/types/play.ts"
      provides: "Shared interaction and placement UI contracts for later pointer-session work"
      contains: "export type DragPhase"
    - path: "src/App.tsx"
      provides: "App-owned placement state keyed by puzzle id"
      contains: "placementsByPuzzleId"
    - path: "src/components/PuzzleShell.tsx"
      provides: "Shell-to-board prop wiring for committed placements and empty-state copy"
      contains: "onPlaceRectangle"
    - path: "src/components/PuzzleBoard.tsx"
      provides: "Board props for committed placements while preserving current rendering baseline"
      contains: "placedRectangles"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/PuzzleShell.tsx"
      via: "placedRectangles and onPlaceRectangle props"
      pattern: "placedRectangles=\\{currentPlacements\\}|onPlaceRectangle=\\{handlePlaceRectangle\\}"
    - from: "src/components/PuzzleShell.tsx"
      to: "src/components/PuzzleBoard.tsx"
      via: "prop forwarding"
      pattern: "placedRectangles=\\{placedRectangles\\}|onPlaceRectangle=\\{onPlaceRectangle\\}"
    - from: "src/App.tsx"
      to: "src/rules/index.ts"
      via: "analyzeBoard and analyzePlacement imports"
      pattern: "from \"@/rules\""
---

<objective>
Establish the typed placement-state foundation for live rectangle placement before any high-frequency pointer logic lands.

Purpose: Give Phase 3 one source of truth for committed rectangles so direct manipulation, later correction controls, and solve tracking all build on the same app-level state.
Output: Shared interaction contracts, App-owned placements keyed by puzzle id, and shell/board props ready for the live gesture plan.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-rectangle-placement/03-CONTEXT.md
@.planning/phases/03-live-rectangle-placement/03-RESEARCH.md
@.planning/phases/03-live-rectangle-placement/03-UI-SPEC.md
@.planning/phases/03-live-rectangle-placement/03-VALIDATION.md
@.planning/phases/01-starter-pack-board-shell/01-04-SUMMARY.md
@.planning/phases/02-deterministic-rectangle-rules/02-02-SUMMARY.md
@.planning/phases/02-deterministic-rectangle-rules/02-03-SUMMARY.md
@src/App.tsx
@src/components/PuzzleShell.tsx
@src/components/PuzzleBoard.tsx
@src/types/puzzle.ts
@src/types/rules.ts
@src/rules/index.ts

<interfaces>
From src/types/rules.ts:
```typescript
export interface CellCoord {
  row: number;
  col: number;
}

export interface CandidatePlacement {
  origin: CellCoord;
  rectangle: RectangleBounds;
}

export interface PlacementAnalysis {
  placement: CandidatePlacement;
  primaryIssue: PlacementIssue | null;
  rectangleArea: number;
  coveredCellKeys: string[];
  isValid: boolean;
}

export interface BoardAnalysis {
  isSolved: boolean;
  placements: PlacementAnalysis[];
  invalidPlacements: PlacementAnalysis[];
  uncoveredCellKeys: string[];
  overlappingCellKeys: string[];
}
```

From src/rules/index.ts:
```typescript
export { analyzePlacement, getPrimaryPlacementIssue, isPlacementValid, analyzeBoard, isBoardSolved };
```

From src/components/PuzzleShell.tsx:
```typescript
interface PuzzleShellProps {
  canGoPrevious: boolean;
  canGoNext: boolean;
  onPrevious: () => void;
  onNext: () => void;
  puzzle: PuzzleListItem;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define live-placement UI contracts and create the Wave 0 interaction test scaffolding</name>
  <files>src/types/play.ts, src/components/PuzzleBoard.tsx, src/components/PuzzleShell.tsx, src/components/__tests__/testGeometry.ts, src/components/__tests__/PuzzleBoard.interaction.test.tsx, src/components/__tests__/App.placement.test.tsx</files>
  <read_first>src/types/rules.ts, src/components/PuzzleBoard.tsx, src/components/PuzzleShell.tsx, src/components/__tests__/PuzzleBoard.test.tsx, src/components/__tests__/App.test.tsx, .planning/phases/03-live-rectangle-placement/03-CONTEXT.md, .planning/phases/03-live-rectangle-placement/03-RESEARCH.md, .planning/phases/03-live-rectangle-placement/03-VALIDATION.md</read_first>
  <action>Create `src/types/play.ts` with explicit Phase 3 UI contracts per D-01, D-02, D-03, D-09, D-14, and D-15: `export type DragPhase = "idle" | "armed" | "dragging" | "snapback" | "settle"`, `export type ReleaseFeedback = "none" | "invalid-snapback" | "valid-settle"`, `export interface PointerDragSession { pointerId: number; pointerType: string; origin: CellCoord; startClientX: number; startClientY: number; currentCell: CellCoord; phase: "armed" | "dragging"; preview: PlacementAnalysis | null; }`, and `export interface LivePreviewStatus { area: number; tone: "valid" | "invalid"; text: string; primaryIssue: PlacementIssue | null; }`. Update `PuzzleBoardProps` so the component accepts `placedRectangles?: CandidatePlacement[]` and `onPlaceRectangle?: (placement: CandidatePlacement) => void` while preserving the current read-only rendering when those props are omitted. Keep preview status chips and release-feedback timers board-local for Phase 3; do not add `boardStatusText` or `releaseFeedback` as shell-forwarded props. Update `PuzzleShellProps` to accept only the placement props that App owns and forward `placedRectangles` plus `onPlaceRectangle` directly to `PuzzleBoard`; do not move any rules logic or drag-state ownership into `PuzzleShell`. In `src/components/__tests__/testGeometry.ts`, create the Wave 0 board-rect helper that returns `{ left: 0, top: 0, width: 320, height: 320, right: 320, bottom: 320, x: 0, y: 0, toJSON() {} }`. In `src/components/__tests__/PuzzleBoard.interaction.test.tsx`, create the Wave 0 interaction suite with placeholder coverage points named `arms from clue cell`, `updates preview during drag`, `shows validity state`, `updates without debounce`, `commits on valid release`, and `shows area cue and release feedback`; those tests may start as `test.todo(...)` in this plan but the file and exact case names must exist before Wave 2. In `src/components/__tests__/App.placement.test.tsx`, create a starter suite that at minimum contains `renders the empty placement prompt before the first placement` so Phase 3 foundation changes have a direct verification target in Wave 1.</action>
  <acceptance_criteria>
    - `src/types/play.ts` contains `export type DragPhase = "idle" | "armed" | "dragging" | "snapback" | "settle"`.
    - `src/types/play.ts` contains `export interface PointerDragSession`.
    - `src/components/PuzzleBoard.tsx` contains `placedRectangles?: CandidatePlacement[]`.
    - `src/components/PuzzleBoard.tsx` contains `onPlaceRectangle?: (placement: CandidatePlacement) => void`.
    - `src/components/PuzzleShell.tsx` contains `placedRectangles?: CandidatePlacement[]`.
    - `src/components/PuzzleShell.tsx` passes `placedRectangles` and `onPlaceRectangle` to `PuzzleBoard`.
    - `src/components/PuzzleBoard.tsx` does not add `boardStatusText?: string`.
    - `src/components/PuzzleBoard.tsx` does not add `releaseFeedback?: ReleaseFeedback`.
    - `src/components/__tests__/testGeometry.ts` contains `width: 320`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `arms from clue cell`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `commits on valid release`.
    - `src/components/__tests__/App.placement.test.tsx` contains `renders the empty placement prompt before the first placement`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PuzzleBoard.interaction.test.tsx src/components/__tests__/App.placement.test.tsx</automated>
  </verify>
  <done>The board and shell expose typed placement-aware props, and the Wave 0 geometry plus interaction test files exist before Wave 2 depends on them.</done>
</task>

<task type="auto">
  <name>Task 2: Move committed rectangle ownership into App and add the empty-board placement prompt</name>
  <files>src/App.tsx, src/components/PuzzleShell.tsx, src/components/PuzzleBoard.tsx, src/components/__tests__/App.placement.test.tsx</files>
  <read_first>src/App.tsx, src/components/PuzzleShell.tsx, src/components/PuzzleBoard.tsx, src/components/__tests__/App.placement.test.tsx, src/data/starterPack.ts, src/rules/index.ts, .planning/phases/03-live-rectangle-placement/03-UI-SPEC.md</read_first>
  <action>Update `App.tsx` so committed placements live in `const [placementsByPuzzleId, setPlacementsByPuzzleId] = useState<Record&lt;string, CandidatePlacement[]&gt;>({})`. Derive `const currentPlacements = placementsByPuzzleId[currentPuzzle.id] ?? []`. Add `handlePlaceRectangle(placement: CandidatePlacement)` that appends only when `analyzePlacement(currentPuzzle.size, currentPuzzle.clues, placement, currentPlacements).isValid` is `true`; ignore invalid callbacks so App remains the final guard for PLAY-04. Also compute `const currentBoardAnalysis = analyzeBoard(currentPuzzle.size, currentPuzzle.clues, currentPlacements)` for future phases, even if Phase 3 does not yet render solved-state UI. Pass `placedRectangles={currentPlacements}` and `onPlaceRectangle={handlePlaceRectangle}` through `PuzzleShell`. Add the exact empty-board copy from `03-UI-SPEC.md` when `currentPlacements.length === 0`: heading `No rectangles placed yet` and body `Drag from a numbered clue to preview a rectangle. Release when the area matches the clue and the outline turns valid.` Keep this copy compact inside the existing shell and do not add side panels, legends, or completion UI. Extend `src/components/__tests__/App.placement.test.tsx` so Wave 1 directly verifies the new foundation with tests named `renders the empty placement prompt before the first placement` and `keeps placements scoped by puzzle id`; the second test may call the real `onPlaceRectangle` callback through a small harness component if direct pointer interaction is not available yet, but it must assert that puzzle A and puzzle B keep separate placement arrays.</action>
  <acceptance_criteria>
    - `src/App.tsx` contains `placementsByPuzzleId`.
    - `src/App.tsx` contains `analyzePlacement(currentPuzzle.size, currentPuzzle.clues, placement, currentPlacements)`.
    - `src/App.tsx` contains `analyzeBoard(currentPuzzle.size, currentPuzzle.clues, currentPlacements)`.
    - `src/App.tsx` contains `No rectangles placed yet`.
    - `src/App.tsx` contains `Drag from a numbered clue to preview a rectangle. Release when the area matches the clue and the outline turns valid.`.
    - `src/App.tsx` passes `placedRectangles={currentPlacements}` and `onPlaceRectangle={handlePlaceRectangle}`.
    - `src/components/__tests__/App.placement.test.tsx` contains `keeps placements scoped by puzzle id`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/App.placement.test.tsx</automated>
  </verify>
  <done>Committed placements are keyed by puzzle id in App, the shell shows the Phase 3 empty-board prompt before any placement exists, and PuzzleBoard receives real placement data plus a commit callback from App.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/components/__tests__/App.test.tsx src/components/__tests__/PuzzleBoard.test.tsx` after each task.
Run `npm test -- --run` once before closing the plan to confirm the Phase 3 foundation does not regress Phase 1 or Phase 2 behavior.
</verification>

<success_criteria>
App owns committed placements per puzzle id, shell/board props are placement-aware and typed, and the empty-board prompt appears without changing the premium board-first layout.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-rectangle-placement/03-live-rectangle-placement-01-SUMMARY.md`
</output>
