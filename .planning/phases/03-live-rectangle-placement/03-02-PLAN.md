---
phase: 03-live-rectangle-placement
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/PuzzleBoard.tsx
  - src/components/__tests__/testGeometry.ts
  - src/components/__tests__/PuzzleBoard.interaction.test.tsx
  - src/components/__tests__/App.placement.test.tsx
  - src/styles/global.css
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - UX-03
  - UX-04
must_haves:
  truths:
    - "Player can start a rectangle selection only from a clue cell and drag across the board with one shared pointer interaction model."
    - "The board preview shows current area plus immediate valid or invalid feedback during the drag, without debounce."
    - "Releasing a valid preview commits a placed rectangle, while releasing an invalid preview does not commit and shows restrained snap-back feedback."
    - "Placed rectangles remain associated with their puzzle, disappear when navigating away, and reappear when returning."
  artifacts:
    - path: "src/components/PuzzleBoard.tsx"
      provides: "Pointer-session state, preview rendering, and release handling"
      contains: "onPointerDown"
    - path: "src/components/__tests__/PuzzleBoard.interaction.test.tsx"
      provides: "Focused interaction coverage for preview, validity, and release behavior"
      contains: "updates preview during drag"
    - path: "src/components/__tests__/App.placement.test.tsx"
      provides: "Per-puzzle placement persistence and navigation regression coverage"
      contains: "restores saved placements when returning to a puzzle"
    - path: "src/styles/global.css"
      provides: "Touch-safe board interaction styling and restrained settle/snap-back feedback"
      contains: "touch-action: none"
  key_links:
    - from: "src/components/PuzzleBoard.tsx"
      to: "src/rules/index.ts"
      via: "analyzePlacement on every drag frame"
      pattern: "analyzePlacement\\("
    - from: "src/components/PuzzleBoard.tsx"
      to: "src/App.tsx"
      via: "onPlaceRectangle callback on valid release"
      pattern: "onPlaceRectangle\\("
    - from: "src/components/__tests__/App.placement.test.tsx"
      to: "src/App.tsx"
      via: "pointer-driven placement plus navigation assertions"
      pattern: "Next Puzzle|Previous"
---

<objective>
Implement the actual live rectangle interaction loop: arm from a clue, preview continuously, and commit or reject on release with calm feedback.

Purpose: Satisfy the core direct-manipulation promise of Phase 3 while preserving the Phase 1 shell and Phase 2 rules-first architecture.
Output: Pointer-driven board interaction, visual preview and release feedback, and focused regression coverage for the complete Phase 3 flow.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-rectangle-placement/03-CONTEXT.md
@.planning/phases/03-live-rectangle-placement/03-RESEARCH.md
@.planning/phases/03-live-rectangle-placement/03-UI-SPEC.md
@.planning/phases/03-live-rectangle-placement/03-VALIDATION.md
@.planning/phases/03-live-rectangle-placement/03-01-PLAN.md
@src/components/PuzzleBoard.tsx
@src/App.tsx
@src/styles/global.css
@src/types/play.ts
@src/types/rules.ts
@src/rules/index.ts
@src/components/__tests__/PuzzleBoard.test.tsx
@src/components/__tests__/App.test.tsx
@src/rules/__tests__/ruleTestUtils.ts

<interfaces>
From src/types/play.ts (created in 03-01):
```typescript
export type DragPhase = "idle" | "armed" | "dragging" | "snapback" | "settle";
export type ReleaseFeedback = "none" | "invalid-snapback" | "valid-settle";
export interface PointerDragSession {
  pointerId: number;
  pointerType: string;
  origin: CellCoord;
  startClientX: number;
  startClientY: number;
  currentCell: CellCoord;
  phase: "armed" | "dragging";
  preview: PlacementAnalysis | null;
}
export interface LivePreviewStatus {
  area: number;
  tone: "valid" | "invalid";
  text: string;
  primaryIssue: PlacementIssue | null;
}
```

From src/rules/index.ts:
```typescript
export function analyzePlacement(
  size: number,
  clues: PuzzleClue[],
  placement: CandidatePlacement,
  placedRectangles?: CandidatePlacement[],
): PlacementAnalysis;
```

From src/components/PuzzleBoard.tsx after 03-01:
```typescript
interface PuzzleBoardProps {
  puzzle: PuzzleListItem;
  placedRectangles?: CandidatePlacement[];
  onPlaceRectangle?: (placement: CandidatePlacement) => void;
}
```

Ownership note for this plan:
- `App` owns committed placements and the `onPlaceRectangle` callback.
- `PuzzleBoard` owns live preview status text and local `"valid-settle"` / `"invalid-snapback"` timers; these concerns are not threaded through `PuzzleShell`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement clue-anchored pointer preview with immediate rules-driven status</name>
  <files>src/components/PuzzleBoard.tsx, src/components/__tests__/testGeometry.ts, src/components/__tests__/PuzzleBoard.interaction.test.tsx</files>
  <read_first>src/components/PuzzleBoard.tsx, src/types/play.ts, src/types/rules.ts, src/rules/index.ts, src/rules/__tests__/ruleTestUtils.ts, .planning/phases/03-live-rectangle-placement/03-CONTEXT.md, .planning/phases/03-live-rectangle-placement/03-RESEARCH.md, .planning/phases/03-live-rectangle-placement/03-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `arms from clue cell` proves pointer down on a clue cell creates an armed session but no preview until the movement threshold is crossed.
    - Test 2: `updates preview during drag` proves the first post-threshold move renders an `Area {n}` chip for a rectangle that still contains the origin clue instead of forcing it into the top-left corner.
    - Test 3: `shows validity state` proves an invalid drag renders `Wrong area` and marks preview cells with `data-preview-state="invalid"`.
    - Test 4: `updates without debounce` proves the preview state changes on the same pointer-move sequence rather than after timers.
  </behavior>
  <action>Extend the Wave 0 files created in 03-01. In `src/components/__tests__/PuzzleBoard.interaction.test.tsx`, replace the placeholder coverage with real tests named exactly `arms from clue cell`, `updates preview during drag`, `shows validity state`, and `updates without debounce` using `fireEvent.pointerDown`, `fireEvent.pointerMove`, and the geometry helper. In `src/components/PuzzleBoard.tsx`, implement the shared pointer model per D-01, D-02, D-03, D-04, D-09, D-10, and D-11: clue cells alone can start the session; use `MOUSE_DRAG_THRESHOLD_PX = 6` and `TOUCH_DRAG_THRESHOLD_PX = 10`; after the threshold is crossed call `setPointerCapture(pointerId)` on the board element; map pointer coordinates to clamped board cells from the board grid `getBoundingClientRect()`; build the preview candidate with a non-corner-only rule by enumerating rectangles whose bounds include both the origin clue and the current target cell, discarding out-of-bounds rectangles, then choosing the smallest-area candidate and breaking ties by preferring the candidate whose center is closest to the pointer’s current cell center. That selection rule keeps the clue inside the rectangle rather than forcing it into a corner. Call `analyzePlacement(puzzle.size, puzzle.clues, candidate, placedRectangles ?? [])` on every drag move with no debounce; render preview cells with `data-preview-active="true"`, `data-preview-state="valid"` or `data-preview-state="invalid"`, and `data-preview-origin="true"` on the origin cell; render a compact preview status chip using exact labels `Area {n}`, `Valid`, `Missing clue`, `Contains another clue`, `Wrong area`, `Out of bounds`, and `Overlaps another rectangle`.</action>
  <acceptance_criteria>
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `arms from clue cell`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `updates preview during drag`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `shows validity state`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `updates without debounce`.
    - `src/components/PuzzleBoard.tsx` contains `MOUSE_DRAG_THRESHOLD_PX = 6`.
    - `src/components/PuzzleBoard.tsx` contains `TOUCH_DRAG_THRESHOLD_PX = 10`.
    - `src/components/PuzzleBoard.tsx` contains `setPointerCapture`.
    - `src/components/PuzzleBoard.tsx` contains `data-preview-state`.
    - `src/components/PuzzleBoard.tsx` contains the exact status labels `Wrong area`, `Out of bounds`, and `Overlaps another rectangle`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PuzzleBoard.interaction.test.tsx</automated>
  </verify>
  <done>The board can arm from a clue, enter a live drag after the defined threshold, derive preview validity from `analyzePlacement` on each move, and expose stable DOM hooks plus tests for preview area and validity feedback without forcing the clue into a corner-only rectangle model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit valid releases, animate restrained feedback, and cover puzzle-to-puzzle placement persistence</name>
  <files>src/components/PuzzleBoard.tsx, src/styles/global.css, src/components/__tests__/PuzzleBoard.interaction.test.tsx, src/components/__tests__/App.placement.test.tsx</files>
  <read_first>src/components/PuzzleBoard.tsx, src/styles/global.css, src/App.tsx, src/components/__tests__/PuzzleBoard.interaction.test.tsx, src/components/__tests__/App.test.tsx, .planning/phases/03-live-rectangle-placement/03-CONTEXT.md, .planning/phases/03-live-rectangle-placement/03-UI-SPEC.md, .planning/phases/03-live-rectangle-placement/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `commits on valid release` proves releasing a valid preview appends a placed rectangle and removes the empty-state prompt.
    - Test 2: `shows area cue and release feedback` proves valid release sets settle feedback and invalid release sets snap-back feedback without using destructive red styling.
    - Test 3: `restores saved placements when returning to a puzzle` proves placements disappear on another puzzle and reappear when navigating back.
  </behavior>
  <action>Extend `src/components/__tests__/PuzzleBoard.interaction.test.tsx` with `commits on valid release` and `shows area cue and release feedback`. Add `src/components/__tests__/App.placement.test.tsx` with tests named `removes the empty-state prompt after the first valid placement`, `clears visible placements when navigating to a different puzzle`, and `restores saved placements when returning to a puzzle`; drive those tests through real pointer events and the existing `Next Puzzle`/`Previous` buttons rather than mocking App state. In `src/components/PuzzleBoard.tsx`, on `pointerup` call `onPlaceRectangle(preview.placement)` only when `preview?.isValid` is `true`; then clear the drag session and set local release feedback to `"valid-settle"` for 180ms. For invalid release, do not call the callback; set release feedback to `"invalid-snapback"` for 220ms and clear it after the timer. Clear session and timers on `pointercancel` and when `puzzle.id` changes. Render committed placements from `placedRectangles` with `data-placed-cell="true"` and `data-placed-rectangle="{index}"`. Update `src/styles/global.css` so `.board-grid` has `touch-action: none`; add preview chip, valid preview, invalid preview, placed-region, settle, and snap-back styles that follow D-05 through D-08 and D-12 through D-15. Use `--color-accent` for valid preview and settle, use softened dominant/text/border tones for invalid preview, and do not use `--color-destructive` anywhere in the routine drag feedback path. Keep the board footprint fixed during drag; any status chip must sit inside the board card without changing layout height.</action>
  <acceptance_criteria>
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `commits on valid release`.
    - `src/components/__tests__/PuzzleBoard.interaction.test.tsx` contains `shows area cue and release feedback`.
    - `src/components/__tests__/App.placement.test.tsx` contains `restores saved placements when returning to a puzzle`.
    - `src/components/PuzzleBoard.tsx` contains `data-placed-cell="true"`.
    - `src/components/PuzzleBoard.tsx` contains `invalid-snapback`.
    - `src/components/PuzzleBoard.tsx` contains `valid-settle`.
    - `src/styles/global.css` contains `touch-action: none`.
    - `src/styles/global.css` contains `[data-release-feedback="valid-settle"]`.
    - `src/styles/global.css` contains `[data-release-feedback="invalid-snapback"]`.
    - `src/styles/global.css` does not add `--color-destructive` to preview or release feedback selectors.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/__tests__/PuzzleBoard.interaction.test.tsx src/components/__tests__/App.placement.test.tsx && npm test -- --run</automated>
  </verify>
  <done>Valid release commits a placed rectangle, invalid release does not commit and animates a restrained snap-back, placements remain puzzle-scoped across navigation, and focused plus full-suite tests pass.</done>
</task>

</tasks>

<verification>
Automated:
- `npx vitest run src/components/__tests__/PuzzleBoard.interaction.test.tsx`
- `npx vitest run src/components/__tests__/App.placement.test.tsx`
- `npm test -- --run`

Manual spot-checks after automation:
1. Run the app at desktop width and confirm a valid drag settles quickly without obscuring clue numerals.
2. Run the app at a phone-sized width and confirm dragging does not trigger page scrolling and still feels slightly forgiving on touch.
3. Perform an invalid release and confirm the snap-back is noticeable but restrained, with no harsh destructive-red flash.
</verification>

<success_criteria>
Players can start from a clue, drag a live rectangle preview with immediate area and validity feedback, release to place only when valid, and keep puzzle-scoped placements while navigating between starter-pack puzzles.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-rectangle-placement/03-live-rectangle-placement-02-SUMMARY.md`
</output>
