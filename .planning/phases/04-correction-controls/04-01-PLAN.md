---
phase: 04-correction-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/play.ts
  - src/App.tsx
  - src/components/PuzzleShell.tsx
  - src/components/__tests__/App.placement.test.tsx
  - src/styles/global.css
autonomous: true
requirements:
  - PLAY-05
must_haves:
  truths:
    - "Player can undo more than one committed correction/placement step on the current puzzle (D-04, D-05)."
    - "Player can reset the current puzzle to zero placed rectangles from a compact control row above the board (D-08, D-09)."
    - "Reset asks for confirmation only when at least one rectangle exists; reset on an empty board is immediate no-op (D-06, D-07)."
  artifacts:
    - path: "src/App.tsx"
      provides: "Puzzle-scoped immutable correction history and undo/reset handlers."
      contains: "history stack with past/present snapshots per puzzle id"
    - path: "src/components/PuzzleShell.tsx"
      provides: "Compact action row above board with Undo and Reset controls."
      contains: "Undo button + Reset button wiring"
    - path: "src/components/__tests__/App.placement.test.tsx"
      provides: "Automated coverage for undo stack behavior and smart reset confirmation."
      contains: "undo + reset tests with confirm/cancel paths"
  key_links:
    - from: "src/components/PuzzleShell.tsx"
      to: "src/App.tsx"
      via: "onUndo/onReset callbacks"
      pattern: "onUndo|onReset"
    - from: "src/App.tsx"
      to: "src/components/__tests__/App.placement.test.tsx"
      via: "puzzle-scoped history behavior"
      pattern: "undo|reset|confirm"
---

<objective>
Implement puzzle-scoped correction history and the compact Undo/Reset controls so players can recover from mistakes without losing control of the current puzzle.

Purpose: Deliver the undo/reset portion of PLAY-05 while preserving Phase 03 interaction behavior and puzzle-scoped state ownership.
Output: History-backed correction handlers in App, compact control row in PuzzleShell, and automated tests for undo/reset confirmation behavior.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-correction-controls/04-CONTEXT.md
@.planning/phases/04-correction-controls/04-RESEARCH.md
@.planning/phases/04-correction-controls/04-VALIDATION.md
@.planning/phases/03-live-rectangle-placement/03-live-rectangle-placement-02-SUMMARY.md
@src/App.tsx
@src/components/PuzzleShell.tsx
@src/components/__tests__/App.placement.test.tsx
@src/types/play.ts
@src/styles/global.css

<interfaces>
From src/App.tsx:
```typescript
const [placementsByPuzzleId, setPlacementsByPuzzleId] = useState<Record<string, CandidatePlacement[]>>({});
function handlePlaceRectangle(placement: CandidatePlacement): void;
```

From src/components/PuzzleShell.tsx:
```typescript
interface PuzzleShellProps {
  placedRectangles?: CandidatePlacement[];
  onPlaceRectangle?: (placement: CandidatePlacement) => void;
  emptyPlacementPrompt?: { heading: string; body: string } | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add correction-history contracts and puzzle-scoped snapshot helpers in App (D-04, D-05)</name>
  <read_first>.planning/phases/04-correction-controls/04-CONTEXT.md, src/types/play.ts, src/App.tsx, src/components/__tests__/App.placement.test.tsx</read_first>
  <files>src/types/play.ts, src/App.tsx</files>
  <behavior>
    - Undo pops one snapshot per click and supports at least two consecutive undos when history depth >= 2.
    - Invalid placement attempts do not push history entries.
    - History remains puzzle-scoped by puzzle id.
  </behavior>
  <action>In `src/types/play.ts`, add explicit correction-history types (`PlacementSnapshot`, `PuzzlePlacementHistory`). In `src/App.tsx`, replace direct `placementsByPuzzleId` writes with immutable per-puzzle history state that stores `past: PlacementSnapshot[]` and `present: PlacementSnapshot`. Keep the existing `analyzePlacement(...).isValid` guard as the only placement commit path; push exactly one snapshot on each successful place/remove/reset action per D-05.</action>
  <acceptance_criteria>`rg "PuzzlePlacementHistory|PlacementSnapshot" src/types/play.ts` returns matches; `rg "past:|present:" src/App.tsx` returns history state; `rg "analyzePlacement\\(" src/App.tsx` still gates placement commits.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.placement.test.tsx -t "undo"</automated>
  </verify>
  <done>App owns deterministic multi-step history for each puzzle id and retains the rules-backed valid placement commit gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire compact Undo/Reset action row above the board with smart-confirm reset (D-06, D-07, D-08, D-09, D-10)</name>
  <read_first>.planning/phases/04-correction-controls/04-CONTEXT.md, src/components/PuzzleShell.tsx, src/App.tsx, src/styles/global.css</read_first>
  <files>src/components/PuzzleShell.tsx, src/App.tsx, src/styles/global.css</files>
  <behavior>
    - Undo button is disabled when no past snapshots exist.
    - Reset prompts only when current puzzle has at least one placed rectangle.
    - Control row appears above the board and remains touch-friendly.
  </behavior>
  <action>In `PuzzleShell`, add a compact `puzzle-corrections` action row directly above `PuzzleBoard` containing only `Undo` and `Reset` buttons per D-09. Add new props for `canUndo`, `onUndo`, and `onReset`. In `App`, compute `canUndo` from the current puzzle history and wire handlers. Implement reset smart confirmation with `window.confirm("Reset this puzzle and clear all placed rectangles?")` only when `present.length > 0` per D-06/D-07. In `global.css`, style the row and buttons with minimum 44px hit target and no destructive alert styling per D-03/D-10.</action>
  <acceptance_criteria>`rg "canUndo|onUndo|onReset" src/components/PuzzleShell.tsx` returns prop wiring; `rg "window.confirm\\(\"Reset this puzzle and clear all placed rectangles\\?\"\\)" src/App.tsx` returns one match; `rg "min-height: 44px" src/styles/global.css` includes correction-control selectors.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.placement.test.tsx -t "reset"</automated>
  </verify>
  <done>Undo/Reset controls are rendered in a compact above-board row and reset confirmation behavior is gated strictly by whether placements exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend App placement tests for multi-step undo and smart reset confirmation paths (PLAY-05)</name>
  <read_first>.planning/phases/04-correction-controls/04-VALIDATION.md, src/components/__tests__/App.placement.test.tsx, src/App.tsx</read_first>
  <files>src/components/__tests__/App.placement.test.tsx</files>
  <behavior>
    - At least one test proves two-step undo restores earlier board states in order.
    - Reset tests cover both confirm=true (clears board) and confirm=false (keeps board).
    - Empty-board reset path does not call confirm.
  </behavior>
  <action>Add focused tests named with `undo` and `reset` keywords in `App.placement.test.tsx` that drive real pointer placement interactions, then click action-row buttons. Mock `window.confirm` for reset accept/cancel paths and assert call count is zero on empty board reset. Keep assertions selector-based using existing board data attributes (`data-placed-cell`, `data-placed-rectangle`).</action>
  <acceptance_criteria>`rg "undo" src/components/__tests__/App.placement.test.tsx` and `rg "reset" src/components/__tests__/App.placement.test.tsx` show new PLAY-05 tests; tests assert confirm called on non-empty reset and not called on empty reset.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.placement.test.tsx</automated>
  </verify>
  <done>Undo/reset behavior is regression-protected by automated tests aligned to PLAY-05 and D-04 through D-10.</done>
</task>

</tasks>

<verification>
Run targeted correction-control coverage: `npm test -- --run src/components/__tests__/App.placement.test.tsx`.
Run interaction regression baseline: `npm test -- --run src/components/__tests__/PuzzleBoard.interaction.test.tsx`.
</verification>

<success_criteria>
- Undo can be executed multiple times on the current puzzle and restores prior snapshots deterministically.
- Reset only prompts when placements exist and clears the current puzzle when confirmed.
- Undo/Reset controls remain compact, above the board, and touch-friendly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-correction-controls/04-correction-controls-01-SUMMARY.md`
</output>
