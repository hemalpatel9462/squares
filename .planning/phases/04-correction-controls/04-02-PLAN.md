---
phase: 04-correction-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/App.tsx
  - src/components/PuzzleShell.tsx
  - src/components/PuzzleBoard.tsx
  - src/components/__tests__/App.placement.test.tsx
  - src/components/__tests__/PuzzleBoard.interaction.test.tsx
  - src/styles/global.css
autonomous: true
requirements:
  - PLAY-05
must_haves:
  truths:
    - "Player can remove a placed rectangle directly by tap/click on the board with no remove mode (D-01, D-02)."
    - "Direct removal preserves the calm visual style and does not break drag-preview-release placement behavior (D-03)."
    - "Undo after a removal restores the removed rectangle because removal is tracked in history (D-04, D-05)."
  artifacts:
    - path: "src/components/PuzzleBoard.tsx"
      provides: "Direct rectangle hit removal event path."
      contains: "onRemoveRectangle callback invocation based on data-placed-rectangle"
    - path: "src/App.tsx"
      provides: "History-backed remove handler integrated with undo/reset pipeline."
      contains: "remove callback and immutable snapshot commit"
    - path: "src/components/__tests__/PuzzleBoard.interaction.test.tsx"
      provides: "Regression proof that remove path does not regress drag interactions."
      contains: "remove and drag compatibility assertions"
  key_links:
    - from: "src/components/PuzzleBoard.tsx"
      to: "src/components/PuzzleShell.tsx"
      via: "onRemoveRectangle prop callback"
      pattern: "onRemoveRectangle"
    - from: "src/components/PuzzleShell.tsx"
      to: "src/App.tsx"
      via: "onRemoveRectangle forwarding"
      pattern: "onRemoveRectangle"
    - from: "src/components/__tests__/App.placement.test.tsx"
      to: "src/App.tsx"
      via: "remove then undo flow assertions"
      pattern: "remove|undo"
---

<objective>
Implement direct board rectangle removal and lock regression coverage so remove, undo, reset, and drag interactions work together without behavior drift.

Purpose: Complete PLAY-05 by shipping the direct-removal portion while preserving the Phase 03 placement interaction baseline.
Output: Board-level remove callback wiring, App remove history commits, and tests proving remove + undo + drag compatibility.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-correction-controls/04-CONTEXT.md
@.planning/phases/04-correction-controls/04-RESEARCH.md
@.planning/phases/04-correction-controls/04-VALIDATION.md
@.planning/phases/04-correction-controls/04-01-PLAN.md
@src/App.tsx
@src/components/PuzzleShell.tsx
@src/components/PuzzleBoard.tsx
@src/components/__tests__/App.placement.test.tsx
@src/components/__tests__/PuzzleBoard.interaction.test.tsx
@src/styles/global.css

<interfaces>
From src/components/PuzzleBoard.tsx:
```typescript
interface PuzzleBoardProps {
  puzzle: PuzzleListItem;
  placedRectangles?: CandidatePlacement[];
  onPlaceRectangle?: (placement: CandidatePlacement) => void;
}
```

From src/components/PuzzleShell.tsx:
```typescript
<PuzzleBoard onPlaceRectangle={onPlaceRectangle} placedRectangles={placedRectangles} puzzle={puzzle} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct board tap/click rectangle removal and callback wiring to App (D-01, D-02, D-03)</name>
  <read_first>.planning/phases/04-correction-controls/04-CONTEXT.md, src/components/PuzzleBoard.tsx, src/components/PuzzleShell.tsx, src/App.tsx, src/styles/global.css</read_first>
  <files>src/components/PuzzleBoard.tsx, src/components/PuzzleShell.tsx, src/App.tsx, src/styles/global.css</files>
  <behavior>
    - Pointer/click on a placed cell removes its rectangle immediately with no mode toggle.
    - Removal is ignored when no placed rectangle index exists on the target cell.
    - Removal updates App history so Undo can restore the removed rectangle.
  </behavior>
  <action>Extend `PuzzleBoardProps` with `onRemoveRectangle?: (rectangleIndex: number) => void`. In board event handling, detect placed-cell interaction using `data-placed-rectangle`; call `onRemoveRectangle` with parsed integer index when no drag preview is active. Forward the callback through `PuzzleShell` into `App`. In `App`, implement immutable remove logic against current puzzle `present` snapshot and push prior snapshot to `past` only when index is valid. Add subtle removable affordance in CSS (pointer cursor + light hover tint) without destructive colors per D-03.</action>
  <acceptance_criteria>`rg "onRemoveRectangle" src/components/PuzzleBoard.tsx src/components/PuzzleShell.tsx src/App.tsx` returns wiring; `rg "data-placed-rectangle" src/components/PuzzleBoard.tsx` shows removal path; `rg "cursor: pointer" src/styles/global.css` includes placed-cell removable selector.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.placement.test.tsx -t "remove"</automated>
  </verify>
  <done>Direct interaction on a placed rectangle removes it immediately and the action is recorded in puzzle-scoped history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add remove + undo integration tests and drag-regression guards (PLAY-05)</name>
  <read_first>.planning/phases/04-correction-controls/04-VALIDATION.md, src/components/__tests__/App.placement.test.tsx, src/components/__tests__/PuzzleBoard.interaction.test.tsx, src/components/PuzzleBoard.tsx</read_first>
  <files>src/components/__tests__/App.placement.test.tsx, src/components/__tests__/PuzzleBoard.interaction.test.tsx</files>
  <behavior>
    - App integration test proves remove reduces placed cells and undo restores removed region.
    - PuzzleBoard regression test proves drag preview + valid release still operate after remove support is added.
    - Test names include `remove` for traceability to PLAY-05.
  </behavior>
  <action>In `App.placement.test.tsx`, add remove-flow tests that place rectangles, click a placed cell to remove one rectangle, then click Undo and assert original rectangle cells return. In `PuzzleBoard.interaction.test.tsx`, add/remove-regression assertions ensuring pointer drag from clue still shows preview chip and valid releases still call `onPlaceRectangle` after remove handler exists. Keep geometry mocking via `mockBoardGeometry` and existing data-attribute selectors.</action>
  <acceptance_criteria>`rg "remove" src/components/__tests__/App.placement.test.tsx src/components/__tests__/PuzzleBoard.interaction.test.tsx` returns new PLAY-05 test cases; both targeted test files pass together.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.placement.test.tsx src/components/__tests__/PuzzleBoard.interaction.test.tsx</automated>
  </verify>
  <done>Remove behavior is end-to-end covered and Phase 03 drag-preview-release behavior remains green under regression tests.</done>
</task>

</tasks>

<verification>
Run focused correction and interaction coverage: `npm test -- --run src/components/__tests__/App.placement.test.tsx src/components/__tests__/PuzzleBoard.interaction.test.tsx`.
Run full suite gate: `npm test -- --run`.
</verification>

<success_criteria>
- Tapping/clicking a placed rectangle removes it immediately without a separate mode.
- Undo restores removed rectangles because removal and placement share the same history model.
- Existing drag preview and release behavior remains unchanged under automated regression checks.
</success_criteria>

<output>
After completion, create `.planning/phases/04-correction-controls/04-correction-controls-02-SUMMARY.md`
</output>
