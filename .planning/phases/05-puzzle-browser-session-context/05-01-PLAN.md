---
phase: 05-puzzle-browser-session-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PuzzleBrowser.tsx
  - src/components/__tests__/PuzzleBrowser.test.tsx
  - src/styles/global.css
autonomous: true
requirements:
  - PACK-03
must_haves:
  truths:
    - "Player can switch between Easy, Medium, and Hard in a dedicated browser view (D-01, D-03, D-04)."
    - "Each selected difficulty shows only puzzles from that difficulty (D-02, D-04)."
    - "Player can pick a specific puzzle id from the browser list to enter play (D-01, D-02)."
  artifacts:
    - path: "src/components/PuzzleBrowser.tsx"
      provides: "Segmented difficulty tabs and filtered puzzle list with select callback."
      contains: "tablist+tab semantics and difficulty-filtered puzzle buttons"
    - path: "src/components/__tests__/PuzzleBrowser.test.tsx"
      provides: "Automated regression coverage for tab switching and filtered puzzle rendering."
      contains: "role-based tab and puzzle-list assertions"
    - path: "src/styles/global.css"
      provides: "Browser-panel and segmented-tab styling aligned with the existing shell."
      contains: "puzzle-browser and browser-tab class rules"
  key_links:
    - from: "src/components/PuzzleBrowser.tsx"
      to: "src/data/starterPack.ts"
      via: "difficulty-filtered puzzle list input"
      pattern: "difficulty"
    - from: "src/components/__tests__/PuzzleBrowser.test.tsx"
      to: "src/components/PuzzleBrowser.tsx"
      via: "tab and puzzle-button role queries"
      pattern: "getByRole\\(\"tab\"|getByRole\\(\"button\""
---

<objective>
Build the dedicated browser view component that presents difficulty-first puzzle selection with segmented tabs.

Purpose: Deliver the PACK-03 browser interaction surface before App-level routing and persistence wiring.
Output: A reusable PuzzleBrowser component with automated tests and production styling.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-puzzle-browser-session-context/05-CONTEXT.md
@.planning/phases/05-puzzle-browser-session-context/05-RESEARCH.md
@.planning/phases/05-puzzle-browser-session-context/05-VALIDATION.md
@src/types/puzzle.ts
@src/data/starterPack.ts
@src/components/PuzzleShell.tsx
@src/styles/global.css

<interfaces>
From src/types/puzzle.ts:
```typescript
export type Difficulty = "easy" | "medium" | "hard";

export interface PuzzleListItem extends PuzzleRecord {
  packIndex: number;
  packTotal: number;
  difficultyLabel: "Easy" | "Medium" | "Hard";
}
```

From src/data/starterPack.ts:
```typescript
export const starterPackPuzzles: PuzzleListItem[];
export function getPuzzleById(id: string): PuzzleListItem | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 PuzzleBrowser interaction tests for segmented tabs and filtered list behavior (D-01, D-03, D-04)</name>
  <read_first>.planning/phases/05-puzzle-browser-session-context/05-CONTEXT.md, src/types/puzzle.ts, src/data/starterPack.ts, src/components/__tests__/App.test.tsx</read_first>
  <files>src/components/__tests__/PuzzleBrowser.test.tsx</files>
  <behavior>
    - Test 1: Default selected tab is Easy and only easy puzzle ids render in the list.
    - Test 2: Clicking Medium selects Medium tab and renders only medium puzzle ids.
    - Test 3: Clicking a puzzle button fires onSelectPuzzle with that puzzle id and difficulty.
  </behavior>
  <action>Create `src/components/__tests__/PuzzleBrowser.test.tsx` with role-based tests using `tablist`, `tab`, and puzzle `button` queries. Use starter-pack fixtures from `starterPackPuzzles` and assert exact positive/negative puzzle id visibility per selected tab. Verify callback payload includes `{ puzzleId, difficulty }` where difficulty is one of `easy|medium|hard`.</action>
  <acceptance_criteria>`rg "tablist|role: \"tab\"|onSelectPuzzle" src/components/__tests__/PuzzleBrowser.test.tsx` returns matches; `npm test -- --run src/components/__tests__/PuzzleBrowser.test.tsx` fails before Task 2 implementation.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/PuzzleBrowser.test.tsx</automated>
  </verify>
  <done>Wave 0 tests define PACK-03 browser semantics and fail against missing component implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PuzzleBrowser with Easy/Medium/Hard tabs and difficulty-filtered puzzle actions (D-01, D-02, D-03, D-04)</name>
  <read_first>.planning/phases/05-puzzle-browser-session-context/05-CONTEXT.md, src/types/puzzle.ts, src/data/starterPack.ts, src/components/__tests__/PuzzleBrowser.test.tsx</read_first>
  <files>src/components/PuzzleBrowser.tsx</files>
  <behavior>
    - `Easy`, `Medium`, and `Hard` tabs always render in that order.
    - Selected tab sets `aria-selected="true"` on exactly one tab.
    - Puzzle list shows deterministic ordering by `packIndex` within the selected difficulty.
  </behavior>
  <action>Create `PuzzleBrowser` with props `{ puzzles: PuzzleListItem[]; selectedDifficulty: Difficulty; onSelectDifficulty: (difficulty: Difficulty) => void; onSelectPuzzle: (selection: { puzzleId: string; difficulty: Difficulty }) => void; }`. Render a browser panel with a `tablist` labeled `Browse by difficulty`, three fixed tabs (`Easy`, `Medium`, `Hard`), and a `tabpanel` listing puzzle buttons labeled `Puzzle {id}` for the active difficulty only. On button click, call `onSelectPuzzle` with the selected puzzle id and active difficulty.</action>
  <acceptance_criteria>`rg "Browse by difficulty|Easy|Medium|Hard|onSelectPuzzle" src/components/PuzzleBrowser.tsx` returns matches; `rg "role=\"tablist\"|role=\"tabpanel\"" src/components/PuzzleBrowser.tsx` returns matches; Task 1 tests pass after implementation.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/PuzzleBrowser.test.tsx</automated>
  </verify>
  <done>Dedicated browser component renders segmented difficulty tabs and difficulty-filtered puzzle selection actions required by PACK-03.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser-view styles that preserve existing shell visual language and touch targets (D-01, D-03)</name>
  <read_first>src/styles/global.css, src/components/PuzzleShell.tsx, src/components/PuzzleBrowser.tsx</read_first>
  <files>src/styles/global.css</files>
  <action>Add concrete style blocks for `.puzzle-browser`, `.puzzle-browser__tabs`, `.puzzle-browser__tab`, `.puzzle-browser__tab[data-selected="true"]`, and `.puzzle-browser__list` using existing spacing/color tokens. Set browser tab and puzzle buttons to `min-height: 44px`, keep responsive wrapping under `@media (max-width: 640px)`, and avoid introducing new global element selectors.</action>
  <acceptance_criteria>`rg "\\.puzzle-browser|\\.puzzle-browser__tab|min-height: 44px" src/styles/global.css` returns matches; `npm run build` succeeds.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Browser component has production styling consistent with current shell and remains touch-friendly on mobile.</done>
</task>

</tasks>

<verification>
Run browser component tests: `npm test -- --run src/components/__tests__/PuzzleBrowser.test.tsx`.
Run build verification: `npm run build`.
</verification>

<success_criteria>
- Dedicated browser view exposes Easy/Medium/Hard segmented tabs with accessible tab semantics.
- Only puzzles from the selected difficulty are shown and selectable.
- Browser UI styling is responsive and keeps 44px minimum action targets.
</success_criteria>

<output>
After completion, create `.planning/phases/05-puzzle-browser-session-context/05-puzzle-browser-session-context-01-SUMMARY.md`
</output>
