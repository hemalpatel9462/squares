---
phase: 05-puzzle-browser-session-context
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/App.tsx
  - src/components/PuzzleShell.tsx
  - src/components/PuzzleNavigator.tsx
  - src/components/__tests__/App.browser.test.tsx
  - src/components/__tests__/App.test.tsx
  - src/components/__tests__/App.placement.test.tsx
autonomous: true
requirements:
  - PACK-03
  - SAVE-02
must_haves:
  truths:
    - "Player lands in the dedicated browser first, picks difficulty and puzzle, then enters play (D-01, D-02)."
    - "Last selected difficulty is restored on reload; invalid/missing saved value falls back deterministically (D-05, D-07, D-08)."
    - "While playing, the selected difficulty remains visible and Back to Browser preserves the same difficulty tab (D-09, D-10)."
  artifacts:
    - path: "src/App.tsx"
      provides: "Browser/play view state machine, selectedDifficulty persistence, and fallback policy."
      contains: "activeView state and localStorage hydration/write logic"
    - path: "src/components/PuzzleNavigator.tsx"
      provides: "Explicit back-to-browser action in play view."
      contains: "Back to Browser button and callback prop"
    - path: "src/components/__tests__/App.browser.test.tsx"
      provides: "Requirement-level PACK-03 and SAVE-02 automated coverage."
      contains: "difficulty browse + localStorage restore tests"
  key_links:
    - from: "src/App.tsx"
      to: "window.localStorage"
      via: "selectedDifficulty read/write under one storage key"
      pattern: "localStorage\\.(getItem|setItem)"
    - from: "src/App.tsx"
      to: "src/components/PuzzleBrowser.tsx"
      via: "selectedDifficulty and puzzle selection callbacks"
      pattern: "onSelectDifficulty|onSelectPuzzle"
    - from: "src/components/PuzzleNavigator.tsx"
      to: "src/App.tsx"
      via: "back action callback preserving selected tab"
      pattern: "onBackToBrowser"
---

<objective>
Wire browser-first routing and selected-difficulty persistence into App, with automated coverage for PACK-03 and SAVE-02.

Purpose: Complete Phase 5 by connecting browser UI to gameplay while preserving session context across reloads.
Output: App-level browser/play flow, persisted selected difficulty, back-to-browser path, and updated integration tests.
</objective>

<execution_context>
@/Users/hemalpatel/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hemalpatel/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-puzzle-browser-session-context/05-CONTEXT.md
@.planning/phases/05-puzzle-browser-session-context/05-RESEARCH.md
@.planning/phases/05-puzzle-browser-session-context/05-VALIDATION.md
@.planning/phases/05-puzzle-browser-session-context/05-01-PLAN.md
@src/App.tsx
@src/components/PuzzleShell.tsx
@src/components/PuzzleNavigator.tsx
@src/components/PuzzleMeta.tsx
@src/components/__tests__/App.test.tsx
@src/components/__tests__/App.placement.test.tsx
@src/data/starterPack.ts
@src/types/puzzle.ts

<interfaces>
From src/types/puzzle.ts:
```typescript
export type Difficulty = "easy" | "medium" | "hard";
```

From src/data/starterPack.ts:
```typescript
export function getPuzzleById(id: string): PuzzleListItem | undefined;
export function getPuzzleByIndex(index: number): PuzzleListItem | undefined;
export const starterPackPuzzles: PuzzleListItem[];
```

From src/components/PuzzleShell.tsx:
```typescript
interface PuzzleShellProps {
  canGoPrevious: boolean;
  canGoNext: boolean;
  onPrevious: () => void;
  onNext: () => void;
  puzzle: PuzzleListItem;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add App browser-session tests for difficulty browsing, persistence restore, and back-path continuity (PACK-03, SAVE-02)</name>
  <read_first>.planning/phases/05-puzzle-browser-session-context/05-VALIDATION.md, src/components/__tests__/App.test.tsx, src/App.tsx, src/data/starterPack.ts</read_first>
  <files>src/components/__tests__/App.browser.test.tsx</files>
  <behavior>
    - Test 1: Player can switch `Easy` -> `Medium` -> `Hard`, open a puzzle, and see matching difficulty in play metadata.
    - Test 2: Saved `medium` difficulty in localStorage is restored on initial render.
    - Test 3: Invalid saved value falls back to current puzzle difficulty if available, else `easy`.
    - Test 4: Back to Browser returns to the previously selected difficulty tab.
  </behavior>
  <action>Create `App.browser.test.tsx` with PACK-03/SAVE-02 test names matching 05-VALIDATION.md wording. Use `window.localStorage.setItem` preloads and `cleanup()` remount behavior to simulate reload. Assert selected tabs using role queries (`getByRole("tab", { name: "Medium", selected: true })`) and assert play metadata text includes the selected difficulty label.</action>
  <acceptance_criteria>`rg "browses puzzles by difficulty|restores selected difficulty from localStorage|Back to Browser" src/components/__tests__/App.browser.test.tsx` returns matches; running this file fails before Task 2 implementation.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.browser.test.tsx</automated>
  </verify>
  <done>App-level requirement tests exist and fail until browser-routing + persistence behavior is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement browser-first App flow, selectedDifficulty persistence, and back-to-browser wiring (D-01 through D-10)</name>
  <read_first>.planning/phases/05-puzzle-browser-session-context/05-CONTEXT.md, src/App.tsx, src/components/PuzzleShell.tsx, src/components/PuzzleNavigator.tsx, src/components/PuzzleBrowser.tsx, src/types/puzzle.ts</read_first>
  <files>src/App.tsx, src/components/PuzzleShell.tsx, src/components/PuzzleNavigator.tsx</files>
  <behavior>
    - App initial view is browser, not play.
    - App stores selected difficulty under a single key `squares.selectedDifficulty.v1`.
    - Storage fallback precedence is: valid stored difficulty -> current selected puzzle difficulty -> `easy`.
  </behavior>
  <action>In `App.tsx`, add `activeView` as `"browser" | "play"` defaulting to `"browser"` per D-01, and add `selectedDifficulty` state typed as `Difficulty`. Hydrate `selectedDifficulty` from localStorage key `squares.selectedDifficulty.v1` with strict validation (`easy|medium|hard`) per D-05; if invalid or missing apply fallback precedence per D-07/D-08. Persist updates via effect with `localStorage.setItem`. Render `PuzzleBrowser` when in browser view; selecting a puzzle must set current puzzle index by id, update selectedDifficulty to that puzzle difficulty, and switch to play. In play view keep difficulty visible via existing `PuzzleMeta` and add `Back to Browser` action in `PuzzleNavigator`/`PuzzleShell` that flips only `activeView` and preserves selected tab per D-10. Do not persist full route/view state per D-06.</action>
  <acceptance_criteria>`rg "squares.selectedDifficulty.v1|activeView|localStorage.getItem|localStorage.setItem" src/App.tsx` returns matches; `rg "Back to Browser|onBackToBrowser" src/components/PuzzleNavigator.tsx src/components/PuzzleShell.tsx` returns matches; Task 1 tests pass after implementation.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run src/components/__tests__/App.browser.test.tsx</automated>
  </verify>
  <done>Browser-first flow and selected-difficulty session restore satisfy PACK-03 and SAVE-02 while preserving in-play context visibility and back-path continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update existing App and placement regressions for browser-first entry so full suite remains green</name>
  <read_first>src/components/__tests__/App.test.tsx, src/components/__tests__/App.placement.test.tsx, src/components/__tests__/App.browser.test.tsx, src/App.tsx</read_first>
  <files>src/components/__tests__/App.test.tsx, src/components/__tests__/App.placement.test.tsx</files>
  <behavior>
    - Legacy App tests enter play through browser instead of assuming immediate board render.
    - Placement tests start from browser, open a puzzle, then run existing placement/remove/undo/reset flows unchanged.
    - No regressions in previous phases after browser-first routing.
  </behavior>
  <action>Add shared helper steps in `App.test.tsx` and `App.placement.test.tsx` to click a puzzle from the browser before board assertions. Keep previous assertions for navigation, metadata, remove/undo/reset, and puzzle-scoped placement persistence intact after entering play. Do not delete prior regression cases; adapt setup only.</action>
  <acceptance_criteria>`rg "open puzzle|Back to Browser|Puzzle easy-001" src/components/__tests__/App.test.tsx src/components/__tests__/App.placement.test.tsx` shows browser-entry setup; full suite passes.</acceptance_criteria>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>Existing integration coverage remains valid under browser-first entry with no dropped regression scope.</done>
</task>

</tasks>

<verification>
Run requirement-level tests: `npm test -- --run src/components/__tests__/App.browser.test.tsx`.
Run full regression suite: `npm test -- --run`.
</verification>

<success_criteria>
- Player starts in browser, selects by difficulty, and enters the chosen puzzle.
- Selected difficulty restores after reload from localStorage with deterministic fallback behavior.
- Play view keeps difficulty context visible and offers a clear Back to Browser path that preserves selected tab.
</success_criteria>

<output>
After completion, create `.planning/phases/05-puzzle-browser-session-context/05-puzzle-browser-session-context-02-SUMMARY.md`
</output>
