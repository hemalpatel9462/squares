import { describe, test } from "vitest";

describe("PuzzleBoard live rectangle placement", () => {
  test.todo("arms from clue cell");
  test.todo("updates preview during drag");
  test.todo("shows validity state");
  test.todo("updates without debounce");
  test.todo("commits on valid release");
  test.todo("shows area cue and release feedback");
});
